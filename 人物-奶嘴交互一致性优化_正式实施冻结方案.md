# 人物—奶嘴交互一致性优化项目
## 正式实施技术方案

| 文档项 | 内容 |
|---|---|
| 文档状态 | 正式实施候选方案；完成合同边界确认与 Technical Preflight 后冻结 |
| 一期目标 | 固定 1 个虚拟人物、固定 1 个奶嘴 SKU，在冻结测试集上验证人物—商品交互一致性 |
| 核心交互 | 含入口、近嘴、手持 |
| PRD 核心验收指标 | **Technical Pass Rate ≥50%**：五维全部 PASS |
| Production Readiness 指标 | **Production Pass Rate**：Technical PASS 且单 Case 人工时间≤冻结阈值 |
| Production Pass 是否作为合同核心指标 | 只有甲方书面签署合同附件后才生效；否则仅作为运营效率 KPI |
| 正式测试集 | 20–30 个冻结案例 |
| 泛化验证 | 首批 ≥10 Case；用于风险复核，不因小样本单轮结果自动触发合同违约 |
| 正式模型 Attempt | 每 Case 最多 2 次 Business Attempt |
| 生产主线 | Case Base Image + Geometry/Grip Anchor + Product Preserve + Contact-area Editing + Product Core Restore |
| 快速路径 | 经稳定性 Benchmark 通过的 Multi-Reference Image Edit |
| Repair 进入正式链条件 | 理论可恢复空间 ≥8pp 且实测 Repair Uplift ≥8pp |
| 基准周期 | 22–29 个有效工作日 |
| 最大容忍周期 | 40 个有效工作日；重大范围扩展另行评估 |
| 核心实施纪律 | 先验证素材与几何，再调用生成模型；失败优先归因、收缩或切路，不无限调 Prompt |

---

# 1. 项目第一性原理

项目真正要锁定的是：

```text
人物身份
商品身份
商品位置 / 姿态
嘴唇 / 手指与商品的接触关系
前后遮挡
场景与光照
```

其中：

```text
商品 SKU
商品角度
商品位置
商品缩放
商品旋转
商品透视
关键遮挡
商品核心结构
```

尽量由确定性系统控制。

生成模型主要负责：

```text
嘴唇自然包裹
手指自然接触
接触阴影
局部皮肤和边缘融合
有限区域补全
```

原则：

> **把模型自由度压缩到“局部自然化”，而不是让模型同时决定商品是什么、在哪里、怎么咬、怎么遮挡。**

---

# 2. 一期范围

## 2.1 包含

- 固定 1 个虚拟人物；
- 固定 1 个奶嘴 SKU；
- 静态图片；
- 已冻结的室内生活场景范围；
- 含入口；
- 近嘴；
- 手持；
- 正面、45° 和经 Preflight 确认可支持的侧面范围；
- Product Preserve；
- 确定性位置微调；
- Mouth / Hand Contact Editing；
- 人工 QC；
- Web 内容生产工具或经 TCO Gate 选择的轻量操作工具；
- 测试、日志、Replay、离线交付与文档。

## 2.2 不包含

- 视频连续一致性；
- 任意人物泛化；
- 任意 SKU 泛化；
- 多人物；
- 未冻结的新场景类别；
- 极端侧后方；
- 复杂双手高难交互；
- 100% 无人工审核；
- 专用 3D 咬合系统；
- 专用 ControlNet 训练；
- 大规模多租户平台；
- 无限期维护与无限模型升级。

---

# 3. 输入边界：Canonical Character Base 与 Case Base Image

这是项目必须在合同中写清楚的边界。

## 3.1 Canonical Character Base

用途：

```text
人物身份参考
嘴部 / 手部无遮挡参考
Landmark / Pose 初始化参考
```

建议至少提供：

| 角度 | 最低要求 |
|---|---:|
| 正脸无遮挡嘴部 | ≥1 |
| 左 45° | ≥1 |
| 右 45° | ≥1 |
| 推荐正脸 | 2–3 |
| 推荐左右 45° | 各 2 |
| 侧脸 | 如纳入一期则各 ≥1 |

要求：

- 无奶嘴遮挡；
- 嘴部清晰；
- 五官清晰；
- 身份一致；
- 无严重运动模糊；
- 无明显不一致美颜。

## 3.2 Case Base Image

每个正式交互 Case 在进入 Product Preserve 前必须存在：

> **已经具备正确人物、正确场景、正确身体姿态、正确镜头构图，但尚未加入奶嘴交互的目标底图。**

Case Base Image 必须满足：

```text
人物 Identity PASS
Scene / Camera 已符合该 Case
嘴部或手部目标区域可见
无奶嘴或无需要替换的错误奶嘴结构
```

## 3.3 Case Base Image 的两种来源

### 模式 A：甲方 / 上游直接提供

这是一期推荐模式。

正式 50% 交互验收从：

```text
Case Base Image
```

作为输入开始。

### 模式 B：本项目额外生成

如果要求系统同时完成：

```text
Character Reference
+ Scene
+ Pose
→ Case Base Image
```

则必须新增：

# Base Image Generation Pipeline

并独立统计：

```text
Base Identity Pass
Base Scene Pass
Base Pose Pass
Base Generation Cost
```

只有 Base Image 通过后才进入交互链。

若合同要求“从原始人物参考到最终交互图”的 End-to-End 通过率，则 Base Generation Fail 必须计入最终分母。

## 3.4 默认合同建议

由于当前项目主题是“人物—奶嘴交互一致性优化”，一期建议：

> **把 Case Base Image 作为冻结输入资产，不把通用人物场景生成能力混入 50% 核心验收。**

如果甲方要求本项目同时负责 Case Base Image 生成，视为范围扩展，需重新评估周期、费用与成功率。

## 3.5 Case Base Readiness Gate

项目不允许在“假设未来能拿到 Case Base Image”的前提下直接开工。

启动时必须满足：

```text
Development Case Base Image：100% 就绪
Validation Case Base Image：100% 就绪
Formal Case Base Image：100% 已存在并完成数量/质量盘点
Generalization Case Base Image：可在 Validation Freeze 前补齐
```

Formal Case 为保持盲测，可由甲方或独立验收人保管具体内容，只向实施团队出具 `Asset Readiness Manifest`。

Manifest 至少包含：

```text
formal_case_count
angle_distribution
interaction_distribution
image_resolution
mouth_or_hand_visibility
asset_hash
readiness_status
```

## 3.6 Case Base Image 不足时的执行路径

### 路径 A：甲方补充

在双方约定的 Cure Period 内补充合格 Case Base Image。

建议：

```text
Cure Period = 3 个工作日
```

最终以合同附件为准。

### 路径 B：增加 Base Image Generation Work Package

若甲方明确要求项目同时生成 Case Base Image，则新增独立工作包：

```text
Character / Scene / Pose Reference
        ↓
Base Image Generation
        ↓
Base Identity QC
Base Scene QC
Base Pose QC
        ↓
Qualified Case Base Image
```

该工作包必须：

- 独立报价；
- 独立周期；
- 独立成功率；
- 不与奶嘴 Contact Pipeline 的 50% 指标混成一个黑箱指标，除非合同明确要求 End-to-End 统计。

### 路径 C：暂停 / 终止

如果：

```text
甲方无法补充
AND
不接受 Base Image Generation 变更
```

则项目停在 Preflight / Input Readiness 阶段。

已完成的：

```text
Asset Audit
Preflight
风险报告
技术边界说明
```

按对应里程碑验收和结算。

此时正式性能义务尚未触发。

---

# 4. 商品资产与非平面物体约束

奶嘴不是平面物体，不能把 Homography 当成真实 3D 旋转。

## 4.1 商品素材

最低提供：

- 正面；
- 左右 45°；
- 左右侧面；
- 背面；
- 透明 PNG；
- 吸吮部分；
- 挡板；
- 孔位；
- 环形结构；
- 标准色；
- 材质；
- 关键比例和尺寸；
- 禁止变化区域标注。

推荐：

- 360° 序列；
- CAD / OBJ / FBX / GLB；
- 工程尺寸图。

## 4.2 Product View Metadata

每个商品角度必须记录：

```json
{
  "asset_id": "SKU001_LEFT45",
  "view_yaw": -45,
  "view_pitch": 0,
  "material_class": "TRANSLUCENT",
  "max_residual_yaw": 12,
  "max_residual_pitch": 8
}
```

`max_residual_*` 为 Development 初始值，最终由 Preflight 校准。

## 4.3 Residual Angle Gate

只有：

```text
abs(target_yaw - asset_yaw) <= max_residual_yaw
AND
abs(target_pitch - asset_pitch) <= max_residual_pitch
```

才允许使用二维 Affine / Homography 做小幅补偿。

否则：

```text
切换更接近角度的商品资产
→ 无资产则补拍
→ 或将该 Case 标记 Conditional
→ 高频需求才评估 3D
```

禁止把正面奶嘴通过大幅 Homography 强扭成侧面。

---

# 5. 商品材质分类与透明硅胶策略

每个 Product Asset 必须标注：

```text
OPAQUE
TRANSLUCENT
TRANSPARENT
REFLECTIVE
```

## 5.1 OPAQUE

适合标准 Product Preserve：

```text
RGBA
+ Geometry
+ Contact
+ Light Match
```

## 5.2 TRANSLUCENT

除常规 RGBA 外，推荐提供：

```text
白底
灰底
黑底
```

拍摄或渲染素材，用于辅助估计：

```text
Alpha / Transmission
Specular Highlight
Stable Color Region
```

生产链至少分离：

```text
Product Base Color
Alpha / Transmission Mask
Specular / Highlight Mask
```

不允许简单把白底摄影中烘焙进去的白色高光直接当作所有场景的正确外观。

## 5.3 TRANSPARENT / 高折射

如果产品真实视觉高度依赖：

```text
背景透射
折射
环境反射
```

则二维 Product Preserve 只能作为 Conditional 路线。

高频多背景生产需求触发：

```text
3D Material Render / Dedicated Rendering
```

专项评估。

## 5.4 Material Preflight

Technical Preflight 必须判断：

```text
当前奶嘴 Material Class
RGBA 直接合成是否产生明显贴图感
黑/灰/白背景资产是否足够
是否必须做 Transmission / Specular 分层
```

---

# 6. Technical Preflight

正式开发前必须完成。

## 6.1 Face / Mouth Preflight

检查：

```text
Face Detection
Mouth Corner
Lip Polygon
Mouth Center
Head Yaw / Pitch / Roll
```

自动失败可降级到：

```text
Face Box + Template
→ Manual Three-point
```

## 6.2 Product Placement Preflight

至少验证：

```text
Front
45°
Side（若纳入一期）
```

不调用 AI，只执行：

```text
Asset Select
Translate
Scale
Rotate
Affine / Residual Homography
Alpha Composite
```

## 6.3 Hand Preflight

如果手持纳入核心验收，必须另外验证：

```text
Hand Detection / Landmark
Palm Center
Thumb / Index Contact
Wrist Orientation
Product Grip Anchor
Finger Occlusion
```

自动失败允许人工 Grip Anchor，但必须统计人工时间。

## 6.4 Material Preflight

验证：

- 透明度；
- 边缘；
- 高光；
- 不同背景合成；
- Product Core Restore 后是否仍有贴图感。

## 6.5 Preflight 失败处理

```text
补素材
→ 调整正式角度分布
→ 收缩范围
→ 或进入明确的 3D / 专项变更
```

不得通过“多试几个模型”绕过素材或几何不可行问题。

---

# 7. Pose 安全区

Face Landmark 的 Z 只能视为相对几何辅助，不视为真实物理深度。

初始安全区：

| Pose | 策略 |
|---|---|
| `|Yaw|≤45°` 且 `|Pitch|≤15°` | Green：自动初始化 |
| `45°<|Yaw|≤60°` 或 `15°<|Pitch|≤25°` | Yellow：必须人工确认 |
| `|Yaw|>60°` 或 `|Pitch|>25°` | Red：不进入一期自动生产主链 |

最终阈值由甲方真实素材 Preflight 冻结。

## 7.1 正式测试 Pose Mix

每个 Formal Case 必须标：

```text
GREEN
YELLOW
RED
```

默认一期建议：

```text
RED = 0
YELLOW <=20%
```

如果甲方要求：

```text
YELLOW + RED >20%
```

则必须先执行：

> **Pose Workload Simulation**

验证：

```text
Placement Time
Occlusion Time
Expert Brush Rate
Production Pass Rate
```

是否仍能满足人工时间 Gate。

若不能：

- 调整人工时间 KPI；
- 降级相关能力；
- 或视为范围变更。

---

# 8. Mouth Pipeline

```text
Case Base Image
→ Face / Mouth Landmark
→ Bite Template
→ Product View Select
→ Product Anchor
→ Placement / Auto Snap
→ Occlusion Preset
→ Product Preserve
→ Mouth Contact Edit
→ Product Core Restore
→ Light Match
→ QC
```

## 8.1 Bite Template

至少：

```text
MOUTH_FRONT
MOUTH_LEFT_45
MOUTH_RIGHT_45
MOUTH_LEFT_SIDE
MOUTH_RIGHT_SIDE
NEAR_MOUTH
```

每个模板记录：

```text
Mouth Axis
Insertion Ratio
Allowed Product Views
Occlusion Presets
Safe Transform Range
```

---

# 9. Hand-held 独立 Pipeline

手持不能只复用 Mouth Pipeline 名称。

## 9.1 Hand Landmark / Anchor

至少使用：

```text
Wrist
Palm Center
Thumb Tip
Thumb IP
Index Tip
Index PIP
Middle Tip
```

自动 Landmark 只用于初始化。

## 9.2 Grip Template

至少定义：

```text
HAND_FRONT_RING
HAND_LEFT45_RING
HAND_RIGHT45_RING
HAND_PINCH
```

每个 Grip Template 记录：

```text
Target Product Anchor
Thumb Contact Zone
Index Contact Zone
Front Finger Layer
Rear Finger Layer
Allowed Product Rotation
Allowed Product Scale
```

## 9.3 Product Grip Anchor

奶嘴商品至少标注：

```text
ring_center
ring_inner_edge
ring_outer_edge
shield_center
```

## 9.4 手持遮挡分层

示例：

```text
Case Base Hand
→ Product Rear Layer
→ Rear Finger
→ Product Ring / Shield
→ Front Finger
→ Contact Shadow
→ Hand Contact Edit
→ Product Core Restore
```

## 9.5 Hand Failure Taxonomy

至少单独记录：

```text
H01 Wrong Grip
H02 Finger Penetration
H03 Product-Finger Occlusion Error
H04 Hand Anatomy Failure
H05 Grip Anchor Drift
H06 Product Rotation Error
```

## 9.6 Hand Repair Evaluation Pool

不能用 Mouth Repair 数据证明 Hand Repair。

Hand Repair 的验证数据必须来自 Formal Acceptance 之外的 Development / Validation Hand Base Case。

推荐最低数据：

```text
Hand Development Base Case >= 8
不同 Hand Base Case >= 5
Hand Failure Output >= 15
单个 Base Case 最多贡献 3 个 Failure Output
```

正式统计仍按 Base Case 聚类，不能把同一 Hand Base 的多个 Seed 当成完全独立业务样本。

## 9.7 Hand 样本不足决策树

### 情况 A：Hand-held 属于 Formal 核心范围

Validation Freeze 前必须满足：

```text
Hand Development Assets 足够
AND
Hand Pipeline Preflight 通过
```

如果 Repair Pool 不足：

#### A1：增加“非 Formal”的 Hand Development Assets

甲方补充新的：

```text
Hand Base Image
Grip Pose
Hand Angle
```

这些只用于研发和 Repair Evaluation：

> **不改变 20–30 个 Formal Case 的分母和统计口径。**

#### A2：Repair 证据不足，但 Attempt 1 Hand Pipeline 可用

允许 Hand Case 保留在 Formal 中，但：

```text
Hand Repair 不进入正式 Attempt 2
```

Attempt 2 只能走：

- 已通过该 Hand 类别稳定性 Benchmark 的 Fast Path；或
- 已证明有效的 Preserve Regenerate。

不得用 Mouth Repair 数据类推。

#### A3：Attempt 1 Hand Pipeline 也不可用

Formal Freeze 前必须二选一：

```text
增加 Hand 专项研发 / 资产
```

或：

```text
双方书面 Scope Change
→ Hand-held 改为 Exploratory / Conditional
```

技术方不得单方面把 Hand 从 PRD 范围删除；甲方也不得在证据不足时要求其按稳定 Production Ready 验收。

### 情况 B：Hand-held 不是 Formal 核心范围

可以作为：

```text
Exploratory Capability
```

单独报告，不计入核心 50% 分母。

该选择必须在 Formal 数据集冻结前书面确认。

---

# 10. Product Placement、Auto Snap 与 Sensitivity Study

## 10.1 Auto Snap

普通运营不从任意位置自由摆放。

系统根据：

```text
Mouth / Grip Center
Template
Product Anchor
Product View
```

生成推荐：

```text
Translation
Scale
Rotation
```

运营只在安全窗口内微调。

## 10.2 Gold Placement

Development / Validation 中建立 Gold Case：

```text
Gold Center
Gold Scale
Gold Rotation
Gold Product View
Gold Occlusion / Grip Template
```

Gold 是项目认可的最佳参考，不宣称是物理世界绝对真值。

## 10.3 Placement Sensitivity Study

操作精度 Gate 不直接拍脑袋冻结。

以 Gold 为中心主动扰动：

### Center

```text
0
±2%
±5%
±8%
±10%
```

单位：

> 嘴宽 / Grip Width 百分比。

### Rotation

```text
0
±3°
±7°
±12°
```

### Scale

```text
0
±5%
±8%
±12%
```

对每档扰动执行完整 Contact Pipeline，测：

```text
Interaction Pass
Product Pass
Production Pass
Human Recovery Time
```

## 10.4 最终 Operation Accuracy Gate

例如：

```text
Center Error P80
Rotation Error P80
Scale Error P80
```

必须来源于 Sensitivity Study 的性能拐点。

此前的：

```text
5% / 7° / 8%
```

只能作为初始测试值，不能在无实测时宣称为最终安全阈值。

---

# 11. Mouth Occlusion 与 Hand Occlusion

## 11.1 Mouth 默认三档

```text
Conservative
Medium
Aggressive
```

普通运营只选择档位。

Expert Brush 仅作为少数例外。

## 11.2 Expert Brush Gate

目标：

```text
使用率 <=20%
平均额外时间 <=60秒
```

超过：

> 优先调整模板，而不是让运营承担更复杂操作。

## 11.3 Hand Occlusion

手持不能复用嘴唇三档。

Hand Template 应预生成：

```text
Rear Finger Mask
Front Finger Mask
Product Contact Zone
```

人工只确认：

```text
Finger Layer Order
Grip Preset
```

必要时再进入 Expert Mask。

---

# 12. Product Preserve、Contact Edit 与 Core Restore

## 12.1 Product Masks

```text
M_product
M_core
M_transition
M_contact
M_occlusion
M_visible_core
```

## 12.2 Mouth Contact

```text
M_contact
=
mouth_roi ∩ dilate(M_product)
```

## 12.3 Hand Contact

```text
M_hand_contact
=
hand_grip_roi ∩ dilate(M_product)
```

## 12.4 Product Core Restore

Contact Edit 后：

```text
Final
=
Edit Output
+
Original Product Pixels on M_visible_core
```

合理遮挡区域不恢复。

---

# 13. Contact-area Editing 模型选择

任何正式生产模型在 Frozen Build 前完成：

```text
Code License
Base Model License
Weight License
Commercial Use
Offline / Deployment Conditions
```

不合规模型：

> 只允许研究对照。

---

# 14. Repair 评估：先算可恢复空间，再做模型实验

## 14.1 Failure Classification

Attempt 1 Fail 必须互斥归类：

```text
A Contact-only
B Product-only
C Contact + Product Mixed
D Identity / Scene / Other Multi-failure
```

Mouth 与 Hand 分开统计。

## 14.2 理论最大可恢复空间

设：

```text
P1 = Attempt 1 Overall Pass Rate
e  = Repair-eligible failure proportion
```

在“所有可修失败都 100% 修成功”的极端理想条件下：

```text
U_max
=
(1 - P1) × e
```

这是：

> **结构性理论最大恢复空间。**

如果：

```text
U_max < 8pp
```

则 Repair 不可能达到 8pp 管理目标：

> **直接停止 Repair 正式化。**

不需要再靠经验 Safety Factor。

## 14.3 Repair Evaluation Pool

Mouth Repair：

```text
≥30 个 Failure Output
来自 ≥10 个不同 Base Case
单个 Base Case 最多贡献 3 个输出
```

Hand Repair 如正式纳入：

```text
单独评估
```

不能把同一个 Case 的大量 Seed 当作大量独立业务样本。

## 14.4 Clustered Statistical Report

以 Base Case 为 cluster。

报告：

```text
Repair Success Point Estimate
Cluster-bootstrap Interval
Overall Uplift Point Estimate
Uplift Bootstrap Interval
```

推荐至少：

```text
1000 次 Base-case Cluster Bootstrap
```

Bootstrap 区间用于表达不确定性，不替代合同点估计。

## 14.5 正式 Repair Gate

正式管理 Gate：

```text
Measured Repair Uplift >= 8pp
```

同时报告 Bootstrap Interval。

不再使用：

```text
U_est × 0.7
```

这种缺乏统计依据的固定安全系数。

## 14.6 8pp 的解释

8pp 是：

> **工程管理阈值，不是统计显著性边界。**

20–30 个 Formal Case 中：

- 1 个 Case 已代表约 3.3–5pp；
- 因此正式报告必须同时给出点估计和区间估计；
- 不得把 8pp 描述成精密模型能力差异。

## 14.7 `U_max <8pp` 且 Fast Path 不稳定时的执行路径

这是正式实施必须提前约定的 No-Go 场景。

### Case 1：Attempt 1 已达到核心目标

如果：

```text
Attempt 1 Technical Pass Rate >=50%
```

即使 Repair 不值得做、Fast Path 也不稳定，项目仍可进入 Formal：

> Repair 被取消，Attempt 2 不保证存在有效路线。

### Case 2：Attempt 1 <50%，Repair 理论上不可补足

如果：

```text
Attempt 1 <50%
AND
U_max <8pp
AND
Fast Path 对关键类别不稳定
```

则：

> **禁止进入 Formal Performance Acceptance。**

必须在 Formal Freeze 前选择：

#### 路径 2A：范围收缩

仅允许在 Formal Freeze 前书面变更：

```text
移除已证明一期不可支持的高风险类别
重新定义能力边界
重新冻结 Formal Case Distribution
```

Formal 已开始后不得删除失败 Case。

#### 路径 2B：新增专项技术路线

例如：

```text
Dedicated Identity Control
Dedicated Scene Fix
Additional Product Assets
3D / Material Route
Base Image Regeneration
```

作为 Scope Change，重新报价、排期和 Validation。

#### 路径 2C：增加第三次 Business Attempt

默认：

> **不允许。**

如甲方坚持增加第三次 Attempt，则视为合同指标变更，必须重新评估：

- 成本；
- 人工时间；
- 成功率；
- 候选公平性；
- Formal Attempt Policy。

#### 路径 2D：Engineering Acceptance 结束一期

若甲方不接受范围收缩与专项变更：

> 按 Engineering Acceptance 交付已完成技术资产、失败根因和能力边界。

不得无限调参。

## 14.8 禁止做法

```text
Formal 已开始后删除高难 Case
为了过 50% 临时增加 Attempt
只展示 Successful Case
看到 Formal Fail 后修改 Workflow 再重跑
```

---

# 15. Attempt State Machine

必须严格区分：

```text
Business Attempt
```

和：

```text
Infrastructure Retry
```

## 15.1 状态

```text
CREATED
→ PREPARED
→ INFERENCE_STARTED
→ INFERENCE_SUCCEEDED
→ OUTPUT_PERSISTED
→ VALID_OUTPUT
```

只有：

```text
VALID_OUTPUT
```

产生后才消耗一个 Business Attempt。

## 15.2 Infrastructure Retry

以下属于 Infra Error：

```text
GPU OOM
Worker Crash
HTTP 5xx
Provider Timeout
Network Failure
Object Storage Failure
```

原则：

```text
每个 Business Attempt
Infra Retry <=2
```

不消耗 Business Attempt。

超过：

```text
SYSTEM_FAIL
```

## 15.3 SYSTEM_FAIL 在 Formal 中的处理

默认：

> SYSTEM_FAIL 计入 Production FAIL。

只有满足预先定义的：

```text
Test Environment Invalid
```

例如甲方 GPU 故障、全局网络 outage，经双方确认后才允许整批测试重新安排。

不得因为单个 Case 失败而无限重试。

## 15.4 幂等

每次提交包含：

```text
idempotency_key
case_id
business_attempt_no
request_hash
```

Worker / Queue 重试不能重复扣除 Attempt。

---

# 16. Failure-aware Attempt Policy

## Attempt 1

默认：

```text
Product Preserve + Contact Edit
```

Fast Path 只有稳定性验证通过的类别才能作为 Attempt 1。

## Attempt 2

### Repair Gate 达标

按 Failure Route：

```text
Mouth Contact → Mouth Repair
Hand Contact  → Hand Repair
Product       → Preserve Regenerate / Core Restore
Identity      → Case Base / Canonical Base Re-run
```

### Repair Gate 不达标

Attempt 2 只可使用：

```text
已通过稳定性 Benchmark 的 Fast Path
```

若该类别 Fast Path 同样不稳定：

> 不保留“为了两次而两次”的无效 Attempt。

---

# 17. AI 输出可归因性与 Replay 边界

Replay 分成两种：

## 17.1 Evidence Replay

这是 Formal 争议处理的权威证据。

必须永久/按合同留存：

```text
原始 Case Base
Pre-AI Composite
正式 AI Output
Mask
Placement
Model / API Metadata
QC
Review
Operation Log
```

> **失败归因首先看当时真实保存的 Formal Output，而不是重新生成一次。**

## 17.2 Reconstruction Replay

用于验证流程和参数可重放：

```text
同输入
同模型版本
同 Seed
同参数
同 Workflow
```

本地模型尽可能开启确定性配置，但不对所有 GPU Kernel 承诺 bit-identical。

闭源 API 不承诺 bit-identical。

## 17.3 Randomness Attribution

正式候选模型在 Frozen Build 前必须执行：

> **Same-input Repeatability Test**

至少：

```text
5 个代表 Case
每 Case 相同输入/参数重复 5 次
```

记录：

```text
Pass / Fail Label Consistency
Product Consistency
Interaction Consistency
```

如果同一请求在相同 Case 上经常出现：

```text
一次 PASS、一次 FAIL
```

说明随机性已经影响业务验收：

> 该模型 / 配置不能作为正式主链，或必须把其稳定性风险明确写入 Route Policy。

API 模型原始正式输出必须保存，不能只保留请求参数。

---

# 18. 闭源 API 版本漂移

如果使用第三方 API：

优先要求：

```text
Pinned Model Version
```

如果提供方不能 Pin：

必须保存：

```text
Provider
Model ID
Provider Revision（如有）
API Region
Request Timestamp
Parameters
Response Metadata
```

供应商发生模型升级时：

```text
视为 External Dependency Change
→ 重跑 Validation
→ 重新确认 Frozen Build
```

不得继续沿用旧 Validation 结论。

---

# 19. 光照、透明材质与融合

## 19.1 确定性色彩迁移

默认使用：

```text
LAB / Linear RGB
Luminance Gain
Mean / Variance Match
Histogram Match
Contact Shadow
```

关键标准色区域由：

```text
M_color_critical
```

保护。

## 19.2 Multi-band / Laplacian

只处理：

```text
Contact ROI + Transition Margin
```

不默认全图运算。

## 19.3 性能 Micro-benchmark

必须在甲方目标硬件测试：

```text
512 ROI
1024 ROI
实际最大 ROI
3 / 4 / 5 / 6 Pyramid Level
Alpha
Feather
Multi-band
```

采集：

```text
P50
P95
Peak Host RAM
CPU
Output Quality
```

## 19.4 性能 Gate

初始目标：

```text
CV Postprocess P95 <=5秒
Multi-band ROI P95 <=2秒
```

端到端：

```text
Attempt 1 P95 <=120秒
```

以**真实完整链路压测**为准。

禁止把各模块 P95 简单相加后冒充 End-to-End P95。

---

# 20. 运营精度、认知负荷与人工时间

普通运营默认只操作：

```text
Auto Snap
Safe-range Translate
Safe-range Scale
Safe-range Rotation
Mouth Occlusion Preset / Hand Grip Preset
```

高级：

```text
Homography
Expert Brush
Manual Anchor
```

## 20.1 Operation Accuracy Gate

最终阈值来自 Placement Sensitivity Study，不提前硬编码。

G4 至少：

```text
3 名非技术运营
×
5 个 Gold Case
```

记录：

```text
Center Error
Rotation Error
Scale Error
Occlusion / Grip Selection
Human Time
Production Pass
```

如果不达标：

```text
Auto Snap
→ 参数限幅
→ Preset
→ 减少自由度
```

而不是增加培训复杂度。

## 20.2 Human Time

建议目标：

```text
Total Human Time <=8分钟 / Case
```

但它是否属于合同核心验收，见第 25 节。

---

# 21. Formal 与 Generalization 统计规则

## 21.1 Formal

20–30 个 Case。

正式报告至少提供：

```text
Observed Technical Pass Rate
Observed Production Pass Rate
Wilson / Appropriate Binomial Interval
```

合同仍可采用点估计判定，但报告必须说明小样本区间宽。

## 21.2 Generalization

首批：

```text
>=10 Case
```

只作为：

> **Generalization Review Set**

若相对 Formal 下降 >20pp：

```text
触发 Review
```

而不是立即自动合同降级。

Review 检查：

```text
Case Difficulty
Pose Mix
Lighting
Failure Distribution
Confidence Interval
Input Quality
```

## 21.3 自动范围降级条件

满足以下之一才自动降级：

### 方式 A

```text
Combined Generalization >=20 Case
AND
Drop >20pp
```

### 方式 B

两个独立、预冻结的 ≥10 Case Generalization Batch：

```text
均下降 >20pp
```

否则：

> 只触发风险 Review，不直接修改合同能力等级。

---

# 22. Pose Mix 与正式测试集

Formal Freeze 前必须输出：

```text
Green Count
Yellow Count
Red Count
```

默认建议：

```text
Red = 0
Yellow <=20%
```

如 Yellow + Red >20%：

必须先验证：

```text
Average Human Time
Expert Brush Rate
Operation Accuracy
Technical Pass
Production Pass
```

若不能满足：

- 修改正式能力声明；
- 调整人工 KPI；
- 或作为范围变更。

---

# 23. Asset Version Graph 与不可篡改证据

每 Case 保存：

```text
Asset Graph
Operation Event
Snapshot
Attempt
Output
Review
```

## 23.1 Atomic Operation Event

记录：

```text
Translate
Scale
Rotate
Homography Point
Occlusion Change
Mask Stroke
Grip Change
Undo / Redo
```

事件包含：

```text
event_id
sequence_no
timestamp
operator
before
delta
after
```

## 23.2 Event Hash Chain

Formal Event 增加：

```text
previous_event_hash
event_hash
```

形成简单 Hash Chain。

## 23.3 Formal Evidence Package

Formal Case 完成后生成：

```text
Evidence Manifest
SHA256
```

内容包含：

```text
Input
Pre-AI Composite
AI Output
Replay Log
Operation Events
QC
Review
Model Metadata
```

## 23.4 WORM / Object Lock

Formal Evidence 应写入：

```text
Versioned Object Storage
+
Retention / Object Lock
```

在约定争议期内：

> 不允许普通管理员修改或删除。

---

# 24. 数据生命周期、TTL 与灾备

## 24.1 TTL 不基于 last_accessed_at 自动延长

推荐：

```text
expire_at
=
stage_completed_at
+
retention_duration
```

只有显式审批：

```text
Extend Retention
```

才延长。

## 24.2 Retention

| Class | 保留 |
|---|---|
| Source | 项目 + 合同归档期 |
| Formal Evidence | `max(180天, 质保期, 争议窗口)` |
| Replay Required | 同 Formal Evidence |
| Validation Intermediate | Freeze 后 90 天 |
| Dev Intermediate | Stage Complete 后 30 天 |
| Scratch | 24 小时 |
| Cache | 7 天 |

## 24.3 Soft Delete

```text
Soft Delete
→ 7天 Grace
→ Hard Delete
→ Metadata Tombstone
```

Formal Evidence / Legal Hold 不得自动删除。

## 24.4 Backup / Disaster Recovery

### PostgreSQL

至少：

```text
Daily Backup
Weekly Full Backup
```

如基础设施允许，增加 WAL / PITR。

初始目标：

```text
RPO <=24小时
RTO <=4小时
```

最终由甲方 IT 确认。

### Object Storage

```text
Versioning
Formal Evidence Backup
Inventory / Checksum Verification
```

## 24.5 Restore Drill

Clean-room 除重新安装外，还必须：

```text
从 Backup 恢复数据库
→ 恢复 1 个 Formal Evidence Package
→ 恢复 Asset Graph
→ 成功执行 Evidence Replay
```

---

# 25. 验收指标与合同不移动球门

PRD 原始五维通过率应得到尊重。

## 25.1 默认合同口径

如果甲方没有另行书面确认：

> **Technical Pass Rate ≥50% 是一期核心性能验收指标。**

Technical Pass：

```text
Product PASS
AND Interaction PASS
AND Identity PASS
AND Scene PASS
AND Overall PASS
```

## 25.2 Production Pass

```text
Production Pass
=
Technical Pass
AND Human Time <=冻结阈值
```

作为：

> **运营效率 KPI / Production Readiness KPI。**

## 25.3 只有书面签署 C-01 才升级

如果双方明确签署：

> `Production Pass Rate ≥50%`

为合同核心指标，则该要求才生效。

否则技术方案不能单方面把原 PRD 50% 改成包含 8 分钟限制的新指标。

---

# 26. TCO / ROI Gate

项目不能只回答“能不能做”，还必须回答“值不值得做”。

## 26.1 成本变量

项目记录：

```text
C_dev       一次性研发 / 外包费用
C_fixed     月固定算力 / 运维
C_ai        每张模型 / API 成本
T_human     每张人工时间
W_human     人工小时成本
C_storage   每张摊销存储成本
C_manual    纯人工修图每张成本
```

系统单张边际成本：

```text
C_system
=
C_ai
+
T_human × W_human
+
C_storage
```

## 26.2 盈亏平衡

若：

```text
C_manual > C_system
```

理论盈亏平衡图片量：

```text
N_break_even
=
C_dev
/
(C_manual - C_system)
```

若：

```text
C_manual <= C_system
```

则单纯从单张成本角度：

> 不存在规模越大越省钱的直接 Break-even。

项目价值必须来自：

```text
速度
标准化
可复制 SKU / 人物
一致性
数据资产
供应链风险降低
```

等战略价值。

## 26.3 Business Scale Gate

在建设完整 Content Web / Queue / DB 平台化能力前，甲方必须提供：

```text
预计月产量
12个月预计总量
预计 SKU 扩展数量
预计人物扩展数量
人工修图当前真实成本
业务响应时间要求
```

输出：

> **TCO & Break-even Report**

## 26.4 两种交付模式

### Lean Delivery

适用于：

```text
规模较低
主要目标是证明技术可行
短期无大量 SKU / 人物扩张
```

交付：

```text
轻量 Web / Desktop Tool
本地文件 / 简化数据库
Workflow
Test Runner
Report
```

不提前建设过重平台。

### Platform Delivery

只有：

```text
业务规模
或
战略复用价值
```

足以支持时才建设：

```text
FastAPI
Queue
PostgreSQL
MinIO
完整 Event / Replay Platform
```

> **不允许因为技术上能做，就默认把 20–30 张验证项目做成重型平台。**

---

# 27. 闭源 API 与模型稳定性

## 27.1 Pinned Version

优先使用可固定版本的模型。

无法 Pin 时记录：

```text
Provider
Model ID
Revision
Region
Request Time
Parameters
Response Metadata
```

模型升级：

```text
External Dependency Change
→ Validation Re-run
```

## 27.2 Same-input Stability

正式候选配置：

```text
5 Base Case × 5 Repeats
```

记录：

```text
Pass/Fail Label Consistency
Identity Consistency
Product Consistency
Interaction Consistency
```

随机性影响业务标签时：

> 该配置不得成为正式主链。

---

# 28. 工程技术栈

根据 TCO Gate 选择 Lean 或 Platform Profile。

## Platform Profile

| 层 | 推荐 |
|---|---|
| Web | React / Vue |
| Canvas | Fabric.js / Konva.js |
| API | FastAPI |
| Queue | Redis + RQ / Celery |
| Workflow | ComfyUI API Format |
| Native Inference | Python / Diffusers |
| Geometry | OpenCV |
| Landmark | Face / Hand Landmarker |
| DB | PostgreSQL |
| Storage | MinIO / S3 |
| Deployment | Docker Compose |
| Test | Python Test Runner |

一期默认不使用 Kubernetes。

---

# 29. 性能与容量 Gate

性能必须在甲方目标环境实测。

## 29.1 初始性能目标

```text
Attempt 1 End-to-end P95 <=120秒
Contact Repair P95 <=90秒
CV Postprocess P95 <=5秒
```

不把各模块 P95 简单相加当作端到端 P95。

## 29.2 Storage Capacity Report

必须测：

```text
Bytes / Attempt
Bytes / Formal Case
Bytes / Replay Evidence
Dev Daily Growth
30-case Formal Estimate
90-day Storage Projection
```

---

# 30. 冻结前 No-Go 决策矩阵

| 场景 | 是否可进入 Formal | 处理 |
|---|---|---|
| Case Base Image 不足 | 否 | 补资产 / Base Generation 变更 / 按 Preflight 结束 |
| Hand 属于核心范围但 Hand Preflight 不通过 | 否 | 补 Hand 资产 / 专项研发 / 书面 Scope Change |
| Attempt 1 ≥50%，但 Repair / Fast Path 不可用 | 可 | 以 Attempt 1 为主，不承诺有效 Attempt 2 |
| Attempt 1 <50%，`U_max ≥8pp` | 条件可 | 完成 Repair Evaluation，Measured ≥8pp 后进入 |
| Attempt 1 <50%，`U_max <8pp`，Fast Path 稳定 | 条件可 | Attempt 2 使用 Fast Path，并先通过 Validation |
| Attempt 1 <50%，`U_max <8pp`，Fast Path 不稳定 | **不可** | Scope 收缩 / 专项变更 / Engineering Acceptance |
| Hand Repair 样本不足但 Hand Attempt 1 可用 | 条件可 | Hand Repair 禁用，Attempt 2 走已验证替代路线 |
| Product Material Preflight 失败 | 否 | 补材质资产 / 3D / 收缩 |
| Residual Angle 超限且无对应资产 | 否 | 补拍 / 3D / Conditional |
| TCO Gate 不支持平台化 | 可 | 切换 Lean Delivery |

---

# 30. 实施阶段与 Gate

## A. Contract / Input Boundary Freeze

冻结：

- Technical vs Production 验收口径；
- Case Base Image 来源；
- Mouth / Hand Scope；
- Pose Mix；
- Material Class；
- TCO 输入。

## B. Technical Preflight

验证：

- Face；
- Hand（如纳入）；
- Product Placement；
- Material；
- Residual Angle。

## C. Model / Stability Benchmark

验证：

- Fast Path；
- Contact Model；
- Same-input Repeatability；
- API Version Risk。

## D. Product Preserve Prototype

包括：

- Mouth Pipeline；
- Hand Pipeline；
- Auto Snap；
- Residual Angle；
- Material Layer；
- Light Match。

## E. Sensitivity / Repair Evaluation

输出：

```text
Placement Sensitivity
Attempt 1 Failure Distribution
U_max
Measured Repair Uplift
Cluster-bootstrap Report
```

## F. Business Scale / TCO Gate

决定：

```text
Lean Delivery
或
Platform Delivery
```

## G. Content Tool

按选定 Profile 建设。

## H. Validation / Frozen Build

冻结：

- Route；
- Model；
- Prompt；
- Workflow；
- Threshold；
- Attempt Policy。

## I. Formal Acceptance

20–30 Case。

不允许 Case 级特调。

## J. Generalization Review

至少 10 Case。

若 >20pp Drop：

> 触发 Review；达到确认条件后再自动降级。

## K. Offline / DR / Replay / Training

包括：

- Install；
- Backup Restore；
- Evidence Replay；
- Clean-room；
- Training。

---

# 32. Formal Attempt 与测试纪律

- 每 Case 最多 2 个 Business Attempt；
- 每 Business Attempt 最多 2 次 Infra Retry；
- VALID_OUTPUT 才消耗 Business Attempt；
- Formal Case 不删除；
- Case ID 不进入 Prompt；
- SYSTEM_FAIL 默认算 Production FAIL，除非测试环境整体被共同认定无效；
- 模型 / Prompt / Workflow / 参数在 Formal 前冻结。

---

# 33. 报告必须包含

## 效果

```text
Technical Pass Rate
Production Pass Rate
Attempt 1 Pass Rate
Repair Uplift
Mouth Pass
Hand Pass
Product Pass
Identity Pass
Scene Pass
```

## 统计

```text
Point Estimate
Binomial Interval
Generalization Review
Repair Cluster-bootstrap Interval
```

## 运营

```text
Human Time
Operation Accuracy
Expert Brush Rate
Pose Mix
System Fail Rate
```

## 成本

```text
Dev Cost
AI / GPU Cost
Human Cost
Storage Cost
Cost / Production Pass
TCO
Break-even Volume
```

---

# 34. 最终停止 / 收缩条件

出现以下任一情况，应停止继续无上限堆技术：

1. Case Base Image 输入边界无法确认；
2. Technical Preflight 失败且无法补素材；
3. Hand 属于核心验收但 Hand Pipeline 无法通过 Preflight；
4. 商品 Residual Angle 无可用资产；
5. 透明材质无法通过二维 Preserve 达到产品和 Scene Gate；
6. `U_max <8pp`；
7. 实测 Repair Uplift <8pp；
8. Fast Path 同样不稳定；
9. Placement Sensitivity 表明运营可达误差区间仍导致大幅 Fail；
10. Yellow / Red Pose 占比使人工成本无法满足合同指标；
11. Attempt 随机性导致同输入业务标签不稳定；
12. CV / Inference 无法达到冻结性能要求；
13. Storage / DR 无法满足正式证据保存要求；
14. TCO Gate 表明平台化无商业合理性；
15. 超过最大合同周期；
16. 新人物 / SKU / 视频 / 3D 等范围扩展。

处理：

```text
收缩 Production Ready 范围
→ 降级 Conditional / Unsupported
→ 切换 Lean Delivery
→ 进入专项变更 / 二期
```

---

# 35. 合同附件最终必须确认

```text
[ ] 50% 核心验收到底采用 Technical Pass 还是双方另签的 Production Pass
[ ] Case Base Image 由谁提供 / 生成
[ ] Hand-held 是否属于 50% 核心 Formal Scope
[ ] Formal Green / Yellow / Red Pose Mix
[ ] Product Material Class 和透明材质边界
[ ] Product Residual Angle Gate
[ ] Business Attempt / Infra Retry 规则
[ ] Repair 8pp Gate
[ ] Generalization 首批 10 Case 只触发 Review
[ ] 自动范围降级需满足确认性条件
[ ] Contact 模型许可证要求
[ ] API 模型版本漂移处理
[ ] Formal Evidence Retention = max(180天, 质保期, 争议窗口)
[ ] Formal Evidence 使用 Object Lock / WORM
[ ] Backup RPO / RTO
[ ] 最大项目周期
[ ] TCO / Break-even Report 是否作为平台化前置 Gate
[ ] Case Base Readiness Gate 的通过条件与 Cure Period
[ ] Case Base Image 不足时：补充 / Base Generation 变更 / 终止三选一路径
[ ] `Attempt 1 <50% + U_max <8pp + Fast Path 不稳定` 时不得进入 Formal Performance Acceptance
[ ] 第三次 Business Attempt 默认禁止，新增即视为合同变更
[ ] Hand-held 若属于核心 Formal Scope，必须提供额外非 Formal Hand Development Assets
[ ] Hand Repair 样本不足时不得用 Mouth Repair 数据类比
[ ] Hand Attempt 1 不可用时，必须在 Formal Freeze 前专项整改或书面降级
```

---

# 36. 最终结论

项目最需要避免的不是“模型效果偶尔不好”，而是：

```text
输入边界不清
手持能力假支持
非平面商品被当成平面
透明材质被普通 PNG 简化
小样本统计被伪精确化
系统失败和业务 Attempt 混淆
PRD 验收球门被技术方案单方面移动
平台建设远超真实业务规模
Replay 被误解为 AI 输出必然可重复
```

正式实施必须坚持：

> **先冻结输入和合同口径，再验证几何与材质；先证明 Attempt 1 和 Repair 有足够可恢复空间，再决定模型路线；先做 TCO，再决定做轻量工具还是完整平台。**

在以下条件满足时：

```text
Case Base Readiness Gate 通过
Mouth / Hand Scope 与 Pipeline 一致
Product Asset / Material 充分
Preflight 通过
Attempt 1 能力已量化
Repair Gate 通过，或有已验证 Fast Path，或 Attempt 1 本身已达核心指标
Formal Attempt 语义清楚
TCO 合理
```

项目具备较高的工程可交付性。

最关键的 Formal No-Go 条件为：

```text
Attempt 1 <50%
AND
U_max <8pp
AND
Fast Path 不稳定
```

出现该条件时，必须在正式验收前进行范围收缩、专项变更或 Engineering Acceptance，禁止直接进入 Formal Performance Acceptance。

如果其中任一 P0 条件失败，应优先：

> **补素材、收缩范围、切换交付模式或进入变更，而不是继续增加模型复杂度。**
