# Studio 界面与持久化进度 — 2026-09-05

> 后续任务队列与开发 QC 已交付，最新进度见 [任务与 QC](2026-09-05-studio-jobs-qc.md)。

状态：`MODEL DEFERRED / STUDIO DEVELOPMENT SLICE DELIVERED`。

**项目整体尚未完成。** 本轮交付的是可操作的本地编辑工作台、持久化草稿和模板版本，
不是完整生产系统或 G5/G6 验收通过。

## 本轮完成

- React + TypeScript Studio，读取 30 个真实 Development Case 和 11 个商品视图。
- 素材筛选、自动定位、画布拖拽、参数调整、方向键微调、撤销/重做。
- 手工多边形遮挡、六类遮罩查看、本地合成预览、ZIP/参数下载。
- PostgreSQL 草稿保存与读取、乐观版本冲突处理、同事务保存记录。
- 模板不可变版本、来源/交互筛选、历史参数恢复及未保存调整提示。
- 保存事件 SHA256 链与模板内容校验，Validation/Formal Case 访问阻断。
- 新增数据库迁移；前端打包进 API 容器；Compose 默认仅绑定本机回环地址。
- CI 增加前端安装、类型/格式检查、状态逻辑测试和构建。

使用说明见 `docs/development/studio-workbench.md`。

## 验证

- 后端完整套件：121 项通过；随后新增静态服务 2 项通过，相关 Studio 回归复核通过。
- 前端：6 项状态/几何测试通过，类型检查、格式检查、生产构建通过。
- PostgreSQL 新迁移执行成功，Alembic metadata drift 检查通过。
- Development/Validation 原有封存验证、G0 哈希验证、G2 固定输入复现通过。
- 没有调用模型 API。浏览器仅打开了本地预览入口，未进行点击或视觉验收。
- 包含前端的最终 API 镜像构建通过：
  `sha256:2960df45c4310a0bb132c739d1454b196f0d6d59f9e6b292d339efef9b0860e6`。
- `tools/smoke_studio_container.py` 在新建的临时 PostgreSQL 数据库执行真实 HTTP 联调：
  HTML/CSS/JS、30 Case、草稿写入/重读、旧版本冲突 409、模板 v1/v2、ZIP 哈希和非开发
  Case 拦截均通过。临时数据库和测试容器已清理，没有写入用户的编辑记录。
- 已更新现有本地 API 服务，在 `http://127.0.0.1:8000/` 提供工作台。最终入口的页面、
  静态文件、素材目录和数据库草稿读取均通过 HTTP 检查；模型开关保持关闭。

## 剩余工作

| 部分 | 当前状态 |
|---|---|
| 素材准入/封存、本地定位与预览 | 已实现研发流程 |
| Studio 编辑、草稿/模板、保存记录 | 本轮已实现；待浏览器交互验收和后续生产约束 |
| 完整任务队列、生产 Attempt/Retry、输出归档 | 待实现编排；现有状态机和 Schema 不是完整流程 |
| QC 审核、分析报告 | 待实现业务流程及冻结口径对齐 |
| 角色权限、审计身份、Object Lock、备份/恢复 | 待完成 |
| Gold/Proximity/自动遮挡、商品角度覆盖缺口 | 待完成 |
| 模型接触修补与材质处理、G2 稳定性测试 | 按用户要求暂缓 |
| Validation/Frozen Build/Formal/最终交付验收 | 尚未开始 |

下一工作块是任务执行与 QC/输出管理。正式能力必须有对应的 Gate/证据，不能用本地
预览成功、单元测试数量或界面按钮替代验收。现有三套保存表仅用于 Development 编辑。
