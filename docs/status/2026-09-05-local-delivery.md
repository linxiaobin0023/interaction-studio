# 本地可用版交付 — 2026-09-05

> 后续修正：登录页样式冲突已修复，四种视口的实际浏览器布局和键盘顺序已验证。详见 `docs/delivery/local-v1/login-layout-fix.md`。

交付范围已经用户明确确认：**先交付本地可用版本，模型继续暂缓**。
当前软件供本机登录、素材编辑、任务处理、结果/QC 和数据保存使用。Formal/G2/G3 等生产验收没有被宣布完成。
交付手册：[本地版操作与恢复说明](../delivery/local-v1/README.md)。

## 完成内容

- 30 个 Development 素材、11 个产品视图、手动定位/遮挡/遮罩、草稿/模板及保存历史。
- 独立后台 worker、PostgreSQL 持久任务/结果、幂等、超时租约/旧进程隔离。
- 产品、交互、身份、场景、整体五维 QC，新版本保留历史，失败说明/结果绑定/冲突保护。
- 唯一账号、登录退出、改密、账号启停、最小角色权限、管理员/Gate Signer 分离。
- 默认启用认证；真实账号进入草稿事件、任务、模板和审核记录；历史匿名记录保持原样。
- 结果/审核证据 ZIP 和离线校验；加密数据库/输入备份、隔离恢复演练和实际取回命令。
- 幂等安装/升级脚本，初始密码私密文件，交付源码/素材归档和 SHA256 清单。

## 验证

- 后端完整测试：142 passed，4 warnings；前端 6 项既有状态逻辑测试通过，类型/格式/构建通过。
- 实际 PostgreSQL + HTTP：登录、401/403、退出撤销、并发幂等、skip-locked、QC 冲突、结果和离线证据通过。
- 迁移 `0004_studio_accounts` 升级和 metadata drift 检查通过，当前项目已升级。
- 当前项目加密备份和恢复通过，包含 1 个账号、141 个输入文件；当前用户库尚无编辑任务。
- 额外有样本备份/恢复通过：1 草稿、2 模板、2 任务/结果、1 QC、2 账号；恢复后证据重放和 HTTP 权限均通过。
- `recover` 保留新建恢复库、输入目录及私密连接文件经过实测，测试副本已清理，没有切换现有数据库。
- 原始 Development/Validation 封存、G0 哈希验证保持通过。
- API/worker 镜像：`sha256:0f75d948993ad26253d2bc550e575ce1a06ed1a4cf9dfa3feaeec4b87c598da5`。
- 隔离安装证据见 `docs/delivery/local-v1/cleanroom.json`，全部临时容器、数据卷和数据库均单独命名后清理。

## 边界

本地版不包含模型接触修补、材质重打光、生产 Attempt/Retry、正式 QC 导入/计分、Object Lock、
PITR/异地备份、正式容量/安全验收或 Formal/Validation 运行。相关项继续保留在生产阶段清单中。
`DEV_MOUTH_011` 仍因残差超限被正确阻断，不绕过原阈值。

未执行浏览器视觉/点击验收；交付手册提供首次操作核对步骤。没有因为自动测试通过而声明人工验收通过。
备份当前手动执行，尚未配置定时任务。默认仅限回环地址使用，不把该配置作为公网生产部署交付。
