# 人物—奶嘴交互一致性优化项目开发计划

> 依据：《人物-奶嘴交互一致性优化_正式实施冻结方案》、`interaction_studio_prototype/index.html`、`产品原型开发映射说明.md`  
> 计划版本：v1.12（用户确认的本地可用版交付）  
> 计划状态：IN EXECUTION — G0 `PASS_INTERNAL`；Development 30/30、Validation 20/20 均已通过输入准入并完成本地哈希封存；G2 输入准备完成，尚未执行生产模型基准测试，仍不可转为 APPROVED FOR FULL IMPLEMENTATION  
> 周期口径：基准 22–29 个有效工作日，最大 40 个有效工作日；范围扩展另行估算

## 1. 目标、范围与验收口径

一期交付一个受控的静态图片生产系统，用于固定 1 个虚拟人物、固定 1 个奶嘴 SKU，在 Mouth、Near-mouth、Hand-held 三类交互中完成：

```text
Case Base Image
→ Geometry / Grip Placement
→ Product Preserve
→ Contact-area Editing
→ Product Core Restore
→ 五维 QC
→ Evidence / Replay
```

一期默认核心性能指标：

```text
Technical Pass Rate >= 50%
= Product、Interaction、Identity、Scene、Overall 五维全部 PASS
```

`Production Pass = Technical Pass AND Total Human Time <= 冻结阈值`，在产品中作为运营效率 KPI 展示。

Formal 测试集为 20–30 个冻结 Case。每 Case 最多 2 个 Business Attempt，每个 Business Attempt 最多 2 次 Infrastructure Retry。Generalization 首批不少于 10 Case，仅触发风险 Review；自动降级必须满足冻结方案中的确认性条件。

明确不纳入一期：视频、任意人物/SKU 泛化、多人物、极端侧后方、复杂双手、专用 3D 咬合、专用 ControlNet 训练、大规模多租户、100% 无人工审核、Base Image Generation。

### 1.1 范围分级

| 级别 | 定义 | 本期处理 |
|---|---|---|
| P0 冻结必做 | 正式方案或原型已明确的生产、算法、测试、证据与质量能力 | 进入一期 DoD 和 22–29 日基准排期 |
| P1 条件增强 | 不是冻结需求，但可在客户环境或合规要求下增强的控制 | 默认不阻断 G5/G6；启用时单独确认依赖和排期 |
| 范围外变更 | 会改变输入、技术路线或产品边界的事项 | 只记录 `OUT_OF_SCOPE_CHANGE` 决策；不在本项目开发 |

P0 包括 Attempt/Retry、Formal Freeze/只读、Readiness/Cure、Preflight/No-Go、三类交互主链、版本/Route/License、QC/分析、SHA256 Hash Chain、Evidence/Object Lock/Replay 和冻结方案要求的 Backup/Restore。P1 默认包括产品内双人盲审/裁决队列、独立 KMS 签名与可信时间戳、额外的专职合规角色和客户未提供统一认证时的 MFA 集成。v1.5 中所有 P1 初始状态均为 `DEFERRED`，不进入当前 Backlog；只有 G0 形成带工期影响的变更记录后才可转为 `ENABLED`。

## 2. 开工与项目时钟规则

### 2.0 当前执行基线（2026-09-05）

用户已明确模型部分暂缓，授权继续模型无关开发。本轮交付 Development 素材目录、
本地定位/手动参数、六类遮罩、五图层与预览导出，不依赖生产模型凭据。
这项研发排期调整不改变 G2/G3 的通过条件，也不开放 Formal 或宣称正式质量达标。
用户进一步确认“先交付本地可用版本，模型继续暂缓”。本地版新增唯一账号/RBAC、
真实操作身份、离线证据包、加密备份/恢复和独立安装验证。
最新证据见 `docs/status/2026-09-05-local-delivery.md`。
已实现本地 Studio、草稿/模板版本、保存记录、持久后台处理任务和开发五维 QC。
生产 Attempt/Retry、正式 QC 导入/分析、正式权限与最终验收仍属于后续生产阶段。

| 项目 | 状态 | 证据/边界 |
|---|---|---|
| Repo、Compose、API、核心 Schema、迁移、CI 测试 | DONE | 属于 G0/G1 可逆研发基础 |
| G0 Scope/Input/Data Boundary | PASS_INTERNAL | 负责人、指标、范围、数据边界和 P1 Deferred 已冻结并绑定证据哈希 |
| Synthetic Mock Manifest | CONDITIONAL | v0.2，27 项资产，`SYNTHETIC_MOCK / PILOT_ONLY` |
| 权威产品母版 | DONE v1 | 参数化 SDF；11 个精确相机视图；RGBA/Depth/Normal/Transmission/Specular |
| Project-owned Development | READY / LOCAL HASH SEALED | 30/30；Mouth 12、Near-mouth 8、Hand 10；30/30 Face、10/10 Hand；实测 Green 24、Yellow 6 |
| 独立 Validation | READY / LOCAL HASH SEALED | 20/20；Mouth 8、Near-mouth 5、Hand 7；20/20 Face、7/7 Hand；实测 Green 16、Yellow 4；输入准入后、参数选择前封存 |
| Face/Mouth Landmark | PASS（Mock） | MediaPipe 0.10.35；13/13 Face、10/10 Case Mouth Geometry |
| Hand Landmark | PASS（Mock） | 8 个独立 Hand Base；8/8 检出 21 点骨架 |
| Product Alpha / Residual | CONDITIONAL（Mock） | 6 个 Alpha View；10/10 Pairwise Residual 通过既定阈值，但产品角度仍是生成目标值而非独立实测 |
| Material Layering Decision | PASS（已作决定） | RGBA 内部透射不足；一期必须保留 Transmission/Specular 层 |
| Development/Validation Manifest | INPUT ADMISSION PASS | `PROJECT_OWNED_SYNTHETIC`；图像/清单哈希、配额、实测姿态、视觉审核、跨集复用校验均通过；本地哈希封存不等同 Object Lock 或完整 G1/G2 通过 |
| Provider/运行环境 | PARTIAL | 数据边界已冻结为外部 API 仅处理合成素材；生产凭据和 G2 固定模型 Benchmark 尚缺 |
| 素材准入与状态 API | DONE | 已接入冻结校验和 Development/Validation 汇总接口；接口不开放 Formal 内容 |
| G2 输入准备 | PREPARED / NOT EXECUTED | 固定 12 个 Development Case，5×5 重复计划，产品视角残差 12/12 通过；尚需固定 Provider、编辑工作流/Mask 和重复性判定规则 |
| 本地定位与遮罩 API / CLI | IMPLEMENTED / DEVELOPMENT ONLY | 三类交互；30 个 Case 中 29 个渲染、1 个角度残差阻断；六遮罩/五图层及哈希清单导出，不代表质量通过 |

上述 PASS 只证明工具链和测量方式可运行，不证明真实产品几何、材质、人物身份或 Formal 通过率。研发侧不再把 Mock 素材数量、Alpha、Landmark Adapter、合成或残差测量列为 G1 阻塞；外部输入不得用内部 Mock 冒充补齐。

### 2.1 开工条件

以下条件全部完成后，计划状态才转为 `APPROVED FOR FULL IMPLEMENTATION`：

1. Technical Pass 与 Production Pass 的计算口径已配置。
2. Case Base 来源、Hand Formal Scope、Pose Mix、Material Class 已书面冻结。
3. Development、Validation、Formal Case Base Readiness Manifest 已通过。
4. 目标硬件、网络、外部 API 使用边界和数据处理边界已确认。
5. 甲方指定 Formal 素材保管/执行人、验收评审人和 Gate Signer；是否分人由 G0 权限矩阵确定。

G0/G1 前只允许进行可逆工作：Repo/CI、接口 Schema、素材校验、技术 Spike、部署环境探测。不得建设完整业务系统。

### 2.2 有效工作日与暂停

- 有效工作日从 G0 签署后的首个工作日起算。
- 素材、账号、硬件或外部 Provider 未就绪时，依赖阶段暂停，排期相应顺延。
- 实施方自身缺陷修复、返工、环境配置错误计入有效工作日。
- 22–29 日为所有前置条件按时满足时的基准；累计超过 29 日进入排期纠偏，达到 40 日必须重新评估范围和计划。
- Formal 开始后禁止为修复单个 Formal Fail 修改 Frozen Build；如需修改，只能重新执行 Validation 并生成新的 Frozen Build。

## 3. 强制 Gate 顺序与技术基线

必须按以下顺序推进：

```text
G0 Scope / Input Boundary Freeze
→ G1 Technical Preflight
→ G2 Model / Stability Benchmark
→ G3 Product Preserve Prototype
→ G4 Sensitivity / Repair Evaluation
→ G5 Content Tool
→ G6 Validation / Frozen Build
→ G7 Formal Acceptance
→ G8 Generalization / DR / Delivery
```

本开发计划直接采用原型对应的 Web 生产工具技术基线：

| 层 | 推荐实现 | 责任 |
|---|---|---|
| Web | React + TypeScript；Konva.js 或 Fabric.js | 原型 11 个页面、Canvas、角色、只读态 |
| API | FastAPI + Pydantic | 领域 API、Gate、Freeze、Attempt 编排 |
| Worker | Redis + RQ | 推理、CV 后处理、批测、证据打包 |
| CV/Inference | Python、OpenCV、Landmarker、ComfyUI/Diffusers Adapter | 定位、Mask、合成、Contact Edit、Core Restore |
| DB | PostgreSQL | 状态机、QC、Gate、事件、审计 |
| Storage | 支持 Versioning/Object Lock 的 S3 或经验证的 MinIO | 资产、中间产物、Formal Evidence |
| Deployment | Docker Compose | 一期部署；默认不引入 Kubernetes |
| Observability | 结构化日志、Metrics、Trace/Request ID | Attempt、Retry、性能、容量、失败追踪 |

闭源模型通过统一 `InferenceProvider` 适配层接入；业务逻辑不得直接绑定单一供应商响应结构。

`interaction_studio_prototype/index.html` 只是视觉和交互验收参考，不是生产代码基座；其 Mock 数据、计时器、本地状态和 `simpleHash` 不得复用于正式系统。生产 Hash 统一由后端使用 SHA256 计算并验证。G0 以 `ADR-001` 确认 Canvas 库、Queue、对象存储和 Inference Provider 的可用性；若替换等价组件，不视为范围变更，但必须保持 API/证据语义一致。

## 4. 核心领域对象

| 对象 | 关键字段/约束 |
|---|---|
| Project / Dataset | DEVELOPMENT、VALIDATION、FORMAL、GENERALIZATION、REPAIR_POOL、REPAIR_EVALUATION、REPEATABILITY |
| DatasetManifest | case_count、interaction/pose 分布、asset hashes、custodian、freeze time |
| FormalRun | formal_run_id、dataset_manifest_hash、build_id、status、invalidation_reason、supersedes_run_id |
| Asset / AssetVersion | 类型、URI、SHA256、版本、分辨率、来源、就绪状态、retention_class |
| ProductView | yaw/pitch、material_class、residual gate、RGBA/Transmission/Specular、Anchor |
| ReadinessManifest | required/ready count、分布、asset_hash、状态 |
| CurePeriod | ACTIVE/RESOLVED/EXPIRED、deadline、missing_assets、selected_action |
| Case | interaction、pose_zone、case_base、product_view、dataset、frozen、build_id |
| TemplateVersion | Bite/Grip/Pose/Occlusion/Anchor 配置；冻结后不可改 |
| Placement | center、scale、rotation、residual、Gold 偏差、safe/edge/unsafe |
| ModelVersion | provider/revision/region、许可证、production_eligibility |
| WorkflowVersion | Prompt、参数、Route、Model、I/O Schema、容器/代码摘要 |
| Attempt | attempt_no、state、request_hash、failure_classification、route_reason |
| InfraRetry | retry_no、error_type、provider metadata；计入 Retry Budget |
| Output | object_key/version、SHA256、validation_status、provider metadata |
| QCProtocol / QCResult | Rubric 版本、五维结果、failure_codes、reviewer（P1 可扩展 adjudication） |
| GateResult | gate_type、PASS/CONDITIONAL/NO_GO、blocking_conditions、evidence refs |
| GateThresholdVersion | metric、formula、sample、decision_rule、source、owner、frozen_at |
| OperationEvent | sequence、before/delta/after、operator、previous_hash、event_hash |
| EvidenceManifest | 输入/输出/Mask/事件/QC/模型版本、SHA256、Object Lock |
| TestRun / AnalysisSpec | Dataset、Build、estimand、denominator、代码/Seed/结果 |
| AuditEvent | actor、role、action、resource、result、request/trace ID、timestamp |

## 5. Attempt 状态机与重试语义

### 5.1 正常状态

```text
CREATED
→ PREPARED
→ INFERENCE_STARTED
→ INFERENCE_SUCCEEDED
→ OUTPUT_PERSISTED
→ OUTPUT_VALIDATING
→ VALID_OUTPUT
```

终止/异常状态：

```text
RETRY_PENDING
OUTPUT_VALIDATION_FAILED
SYSTEM_FAIL
CANCELLED（仅 Development/Validation 且未进入 INFERENCE_STARTED）
```

Formal Attempt 不允许人工取消。

### 5.2 VALID_OUTPUT 冻结定义

`VALID_OUTPUT` 仅表示：

- 对象已持久化且可读；
- SHA256、对象版本、MIME、尺寸和基础解码通过；
- 必需输出与 Metadata 齐全；
- 输出不是空图、截断文件或 Provider 协议错误。

`VALID_OUTPUT` 不包含视觉质量或五维 QC。只要结构完整，即使视觉结果很差，也必须消耗 Business Attempt 并进入 QC，禁止将“不满意”伪装成无效输出。

`OUTPUT_VALIDATION_FAILED` 仅限系统性损坏，归入 Infrastructure Retry Budget。初次执行失败后最多允许 Retry 1、Retry 2；Retry 2 仍失败即进入 `SYSTEM_FAIL`，禁止发起 Retry 3。非重试型协议/安全错误直接进入 `SYSTEM_FAIL`。

### 5.3 幂等与并发

提交必须包含：

```text
idempotency_key
case_id
business_attempt_no
request_hash
expected_resource_version
```

数据库唯一约束至少覆盖：

```text
(formal_run_id, case_id, business_attempt_no)    # Formal
(dataset_run_id, case_id, business_attempt_no)   # 非 Formal
(run_id, case_id, idempotency_key)
```

同一个 Attempt 不得因 HTTP、Queue 或 Worker 重试重复扣减。相同对象的复用仅限相同 `run_id + case_id + business_attempt_no + request_hash`，禁止跨 Business Attempt、FormalRun 或 Build 复用结果。因 Test Environment Invalid 或 Provider Drift 重新执行时必须创建新的 `formal_run_id`，旧 Run 保持不可变。Attempt 2 只有在 Route Policy 返回已验证路线时才能创建；没有有效路线时 Case 以 Attempt 1 结果结束，不创建“为了两次而两次”的 Attempt。

### 5.4 Formal Candidate Selection Policy

Formal 不提供人工“从两个结果中挑最好”的入口，避免看到结果后改变规则。由预先指定的非技术 Formal Operator 按正常生产流程完成单次运营 QC/Failure Classification，该结果触发冻结 Route 并计入 Human Time；验收评审人不参与是否生成 Attempt 2。固定流程：

```text
生成 Attempt 1
→ Infra Retry 耗尽 / SYSTEM_FAIL：Case Final SYSTEM_FAIL，不创建 Attempt 2
→ Formal Operator 对 Attempt 1 做一次计时运营 QC
→ Operator 判定 PASS：Operational Candidate = Attempt 1，禁止生成 Attempt 2
→ Operator 判定 FAIL：按互斥 Failure Classification 查询冻结 Route Policy
   → 无已验证路线：Operational Candidate = Attempt 1
   → 有已验证路线：生成 Attempt 2
      → Attempt 2 VALID_OUTPUT：Operational Candidate = Attempt 2
      → Attempt 2 SYSTEM_FAIL：Case Final SYSTEM_FAIL
→ 生产操作锁定后，按冻结 QC Protocol 对每个实际生成 Output 提交一次验收结果
→ Attempt 1 验收 PASS：Final Scored Candidate = Attempt 1；任何多生成的 Attempt 2 不参与计分
→ Attempt 1 验收 FAIL 且 Attempt 2 存在：Final Scored Candidate = Attempt 2
→ Attempt 1 验收 FAIL 且无 Attempt 2：Case Final FAIL
→ Final Scored Candidate 五维全部 PASS：Case Final PASS；否则 FAIL
```

验收时隐藏 Operator QC、Operational Candidate 和 Route Reason，只提供当前输出、冻结输入和 Rubric 所需参考。P0 基线是每个 Output 一份只追加、不可覆盖的验收结果；评审人数和分歧处理在 G0 的 `QCProtocol` 中冻结。双人盲审、裁决和产品内评审队列属于 P1，未启用时不得出现对应页面、排班或 DoD。系统按预注册规则计算 Final Scored Candidate：只要 Attempt 1 验收 PASS，就只按 Attempt 1 计分。Development/Validation 可以保留候选对比功能，但不得把人工挑优交互带入 Formal。

Formal 统计固定为：

```text
Attempt 1 Pass Rate = Attempt 1 PASS Case / 全部 Formal Case
Repair Success Rate = Repair 后 PASS / 实际进入该 Repair Route 的 Attempt 1 FAIL
Overall Repair Uplift = Repair Rescued Case / 全部对应评估 Base Case
Final Technical Pass = 按预注册规则确定的 Final Scored Candidate 五维全部 PASS
```

上述 PASS/FAIL 统计使用验收结果，不使用 Operator QC 标签。只有同时满足“Attempt 1 验收 FAIL、Attempt 2 为 Repair 输出、Attempt 2 验收 PASS”才记为 Rescued。Operator QC 与验收结果的差异单列为 Operation Accuracy/Route Triage Accuracy；False Fail 会增加无效成本但不能增加 Pass，False Pass 会失去合法 Repair 机会，两者均进入运营报告。G4 必须在不少于 20 个非 Formal 分层输出上报告 False Pass、False Fail 和 Wrong Route；是否阻断由 G0 预先登记的 `GateThresholdVersion` 决定，不允许在 Formal 结果出现后补设阈值。

`SYSTEM_FAIL` 不产生可评审候选：Attempt 1 SYSTEM_FAIL 时 Case 立即终止，计入 Technical/Production FAIL；Attempt 2 SYSTEM_FAIL 时保留已判 FAIL 的 Attempt 1 和系统失败证据，Case Final FAIL。两种情况都不得用剩余 Business Attempt 规避基础设施失败。

Fast Path、Preserve Regenerate、Identity/Case Base Re-run 分别单列 Route，不混入 Repair Success Rate；它们对 Final Technical Pass 的贡献单独报告。Formal 禁止人工跳过已冻结 Route 或为单 Case新增路线。

Identity Route 只能使用原冻结 Case Base、Canonical Asset、Workflow 和参数重新执行已批准流程，不得生成、修补或替换 Case Base。任何导致 Case Base 像素、资产版本或输入哈希变化的操作均视为 Formal 输入变更：当前 Case 按既定结果结束，后续只能通过 Scope Change、新 Dataset 和新 Frozen Build 处理。

### 5.5 FormalRun 状态机

```text
DRAFT
→ READY（Dataset/Build/QC/Analysis Manifest 均验证通过）
→ RUNNING（只允许 Frozen Runner 创建 Attempt）
→ REVIEW_PENDING（所有 Case 已终止且 Evidence Input 齐全）
→ COMPLETED（所有可评审 Output 有冻结结果，统计和 Evidence Ready）
```

任一非终态均可在命中冻结的整批无效条件时进入 `INVALIDATED`；已进入 `COMPLETED` 的 Run 不可重开或覆盖，只能新建带 `supersedes_run_id` 的 Run。`start`/`complete`/作废迁移必须使用乐观锁、幂等键和服务端先决条件；Formal Case 不存在单独“剔除”状态。

## 6. DB 与对象存储一致性协议

数据库和 S3/MinIO 不宣称跨系统 ACID。采用 Saga + Reconciler：

```text
1. DB 创建 PENDING Output 和不可变 object_key
2. Worker 写入 content-addressed 临时对象
3. HEAD/GET 校验 object version、size、SHA256
4. Formal 对象写入预配置 Object Lock Bucket，并在 Put 时携带 Retention/Legal Hold 参数
5. HEAD 校验 Object Lock Mode、Retain Until、Object Version 与 SHA256
6. DB 事务：
   - Output → VALID_OUTPUT
   - Attempt → VALID_OUTPUT
   - 扣减 Business Attempt
   - 写 Outbox/Audit Event
7. 非 Formal 对象执行 Finalize；Formal 对象只允许追加标签，不得覆盖版本
8. Reconciler 扫描：
   - 孤儿对象
   - DB 指向缺失对象
   - 未 Finalize 的 Formal Evidence
```

步骤 6 失败时可幂等重试；仅当 Attempt 与 request_hash 完全相同且 SHA256 一致时复用对象。Formal 对象未成功获得 Object Lock 时不得进入 `VALID_OUTPUT`；Evidence 未校验全部对象锁定状态前只能是 `EVIDENCE_PENDING`，不得显示 Ready。

## 7. Formal 冻结与最小权限

### 7.1 角色

| 角色 | 权限边界 |
|---|---|
| Content Operator / Formal Operator | 执行正常生产操作和单次运营 QC；无 Formal 数据集管理、模型配置或验收改判权 |
| Technical Admin | 配置开发路线；Frozen Build 后不可修改正式配置 |
| Formal Custodian / Executor | 导入并冻结 Dataset Manifest，仅通过 Frozen Runner 执行 Formal；不调模型 |
| Acceptance Reviewer / Gate Signer | 通过只追加通道提交 Formal 结果并签署 Gate；不可重跑或改配置 |
| Auditor permission | 只读访问 Manifest、Audit、Evidence 和报告；是权限集，不默认要求专职人员 |

所有角色必须使用唯一账号，禁止共享账号。P0 最小分离规则为 Technical Admin 不得同时签署 Formal Gate；其余是否分人由 G0 的现实人员和权限矩阵决定。P0 提供本地唯一账号与最小 RBAC；客户要求应用集成 SSO/MFA 时按 P1 处理，已由网关/反向代理统一提供的认证可作为环境依赖复用。对象存储 Retention/Object Lock 由现有基础设施管理员配置，不在产品中新增“Storage Compliance Admin”业务角色。

### 7.2 盲测控制

1. Formal Dataset 在 G0 只提交数量、分布、分辨率和哈希 Manifest，不向研发暴露具体内容。
2. G6 Frozen Build 后由 Custodian 导入并校验；所有读取形成 Audit Event。
3. Formal Runner 固定代码/容器摘要、模型、Prompt、Workflow、Threshold 和 Route Policy。
4. Request Manifest 显式证明 Prompt 不含 Case ID、结果历史、QC 标签或人工难度提示。
5. Formal Case 不得进入 Development、Validation、Repair Pool、Gold Case 或模型训练。
6. Formal 开始后禁止删除 Case、替换输入、追加路线、单 Case 特调或修改 QC Rubric。
7. G6 执行 Dataset Reference Audit：确认 Formal asset hash 未被 Development/Validation/Repair/Training 引用，结果纳入 Evidence Package；该能力是后端校验/导出，不新增产品页面。
8. 验收评审人在提交结论前不得查看运营 QC、Final Candidate 标记或 Route Reason。

## 8. 安全、隐私与外部 Provider

即使人物为虚拟资产，也按受限项目数据处理：

- Web/API 全链 TLS；数据库、对象存储和备份静态加密。
- Secret 使用 Secret Manager/受控环境注入，不进入代码、日志、镜像或 Evidence。
- RBAC 最小权限；Formal 业务权限、对象存储基础设施权限与普通管理权分开授予。
- 审计日志追加写，记录认证、下载、配置、冻结、执行、QC、导出和删除尝试。
- Formal Operation/Event sequence 由服务端事务分配并施加 `(case_id, sequence_no)` 唯一约束；最终 Chain Head 与关键 Audit 摘要写入锁定 Evidence Manifest，避免仅重算整条链即可掩盖篡改。
- G0 明确数据地域、外部 Provider 是否允许接收图片、Provider 留存/训练政策和删除承诺。
- 未获得书面批准时，Formal/客户资产不得发送到外部模型 API。
- 日志和错误消息禁止保存原图、完整 Prompt Secret、认证令牌或不必要的个人信息。
- 备份密钥与生产密钥分离；恢复演练同时验证权限和审计。
- 发布前完成依赖扫描、镜像扫描、权限负向测试和基础 Threat Model。

### 8.1 Provider 版本漂移

优先使用 Pinned Version。无法 Pin 时记录 Provider、Model ID、Revision、Region、Request Time、参数与响应 Metadata。

任一正式 Provider Revision、模型权重、Workflow 依赖或容器摘要改变：

```text
Frozen Build → INVALIDATED_EXTERNAL_CHANGE
→ 禁止继续 Formal
→ 重跑 Validation
→ 生成新的 Frozen Build
```

如果漂移发生在 Formal 中途：当前 `FormalRun` 标记为 `INVALIDATED_EXTERNAL_CHANGE`；已产生的输入、输出、事件和 Evidence 永久保留并明确标注“不得用于正式统计”；重新 Validation 并生成新 Frozen Build 后，以 `supersedes_run_id` 创建新 FormalRun，必须使用完整的原 Formal Dataset 从 Case 1 重新执行，不得只补跑剩余 Case 或混合两个 Run/Build 的结果。可复用原 Operator/评审人，但必须记录其已接触失效 Run，且新 Run 重新随机化顺序并禁止复用旧结论；启用独立备用人员属 P1。Provider 不返回可靠 Revision 时，G2 必须增加批前/批中固定 Canary；无法检测或约束漂移的 Provider 只能标记 Conditional。

## 9. Epic 与 Definition of Done

| Epic | 范围 | Definition of Done |
|---|---|---|
| E1 Asset & Readiness | Canonical、Case Base、Product View、材质、Anchor、Manifest、Cure | Gate 可真实阻断；Manifest 可复算和校验 |
| E2 Case & Freeze | Dataset、Case、Formal RBAC、Frozen Build | UI/API/DB/Runner 四层负向测试通过 |
| E3 Studio & Placement | Mouth/Near/Hand、Auto Snap、Gold、Preset | 超 Gate 被阻断/告警；操作事件完整 |
| E4 Attempt | 状态机、Retry、幂等、SYSTEM_FAIL | 故障注入和并发测试不误扣 Attempt |
| E5 QC | Protocol、五维 Rubric、失败码、Formal Review Intake | 五维全 PASS 才通过；Formal 记录只追加且不可覆盖 |
| E6 Preflight/No-Go | Face/Product/Hand/Material/Residual | Gate 单一数据源；blocking_conditions 可导出 |
| E7 Model/Workflow | 版本、许可、Route、漂移 | 不合规模型/已漂移 Build 无法正式执行 |
| E8 Batch/Analytics | 七类 Dataset、Repair、Sensitivity、Repeatability | 分析按冻结 Spec 可复算 |
| E9 Evidence/Replay | Asset Graph、Event、Hash、WORM、Replay | 每个 Formal Case 可校验、导出、恢复 |
| E10 Deploy/DR | 系统部署、备份、恢复、容量、培训 | Clean-room 和 Restore Drill 通过 |

## 10. 资源、人日与排期

### 10.1 基准团队

本 Web 生产工具的基准配置：

- 产品/项目负责人 0.5 FTE；
- 前端 2 FTE；
- 后端/平台 3 FTE；
- 算法/CV 2 FTE；
- QA 1.5 FTE；
- DevOps/Security 1 FTE；
- 业务侧另提供 Formal 素材保管/执行人、Operator、验收评审人和 Gate Signer 的阶段性投入；角色可兼任，但 Technical Admin 不得兼任 Gate Signer。

实际人员低于基准时按人日重新计算完成日期，不能继续承诺 29 日。

### 10.2 人日估算

| 工作包 | 人日 | 关键依赖 |
|---|---:|---|
| G0 输入与技术边界 | 8 | 业务素材、验收口径 |
| G1–G2 Preflight/Benchmark | 20 | 目标硬件、Provider |
| G3–G4 Preserve/Sensitivity/Repair | 28 | Development/Validation 素材 |
| 共享内核、Schema、CI | 16 | 无 |
| G5 Content Tool | 52 | G4 Route 结论 |
| 安全、Evidence、Replay、DR | 28 | 存储/IT 配置 |
| G6 Validation/Frozen Build | 16 | 功能完成、评审人在位 |
| G7–G8 Formal/报告/交付 | 12 | Formal Custodian、冻结 Dataset |
| **合计** | **180** | 不含外部等待 |

人日按角色的基准分配为：产品/项目 10、前端 35、后端/平台 52、算法/CV 38、QA 26、DevOps/Security 19，合计 180。在 22 日下这些分别占可用容量约 91%、80%、79%、86%、79%、86%；算法、DevOps 和项目负责人是低缓冲角色。任一低缓冲角色延误超过 2 个有效工作日，或 P1 功能被启用，必须重算排期，不通过压缩 Validation/Formal/DR 追赶。

### 10.3 22–29 个有效工作日基准

表中的日期是工作窗口，不代表允许越过前序 Gate：标注重叠的后序阶段只能做数据准备、脚本开发和环境探测；其正式实验、结论签署及不可逆建设必须等待前序 Gate PASS。例如 G1 可在 D2 准备检测脚本，但只有 G0 签署后才可形成有效 Preflight 结果。

| 阶段 | 时间 | 主要任务 | 退出条件 |
|---|---:|---|---|
| G0 边界冻结 | D1–D3 | 输入、角色、数据边界、验收技术口径 | 冻结单与 Readiness Manifest |
| G1 Preflight | D2–D6 | Face/Hand/Product/Material/Residual | Preflight Report |
| G2 Model/Stability | D4–D8 | Contact/Fast Path、5×5 Repeatability、License | 候选路线清单 |
| G3 Preserve Prototype | D6–D11 | Mouth 主链、Hand Spike、Auto Snap、Material/Light | 可运行技术原型 |
| G4 Sensitivity/Repair | D9–D13 | Gold、Operation Test、U_max、Repair Pool/Bootstrap | Accuracy/Repair Gate |
| G5 Content Tool | D13–D22 | 建设 UI/API/Runner、QC、Attempt、Gate | E1–E8 产品功能 DoD |
| Evidence/Security/DR | D16–D24 | Event、Saga、WORM、审计、备份恢复 | E9/E10 技术 DoD |
| G6 Validation/Freeze | D23–D26 | E2E、性能/容量、安全、缺陷修复、冻结 | Frozen Build Manifest |
| G7 Formal | D27–D28 | 20–30 Case 独立执行、冻结验收、Evidence | Formal Report 原始结果 |
| G8 Generalization/Delivery | D28–D29 | >=10 Case、DR/Clean-room、培训、交接 | 验收包与交付签署 |

排期成立条件：G0–G4 均按期通过、正式/泛化素材已就绪、团队按基准配置到岗。D27–D29 是执行和报告生成窗口，不包含外部签署等待。任一条件不成立时重新评估，不允许通过压缩 Formal/QC/DR 来维持日期。

Formal Review SLA：Formal Operator 和至少 1 名验收评审人必须在 G7 前完成 Rubric 走查并锁定时间窗口；每个 Output 的验收结果目标在进入 Formal Review Intake 后 4 个有效小时内完成。Formal 最大 30 Case 、每 Case 最多 2 Output，排期按最多 60 份 Review 计算；G6 必须实测单人不低于 10 Output/小时的评审与导入吞吐，否则延长 G7 而不降低评审要求。如 G0 选择 P1 双人盲审/裁决，需增加评审人、分歧 SLA 和排期，不得在原 22–29 日承诺中默认吸收。

关键路径：

```text
输入就绪
→ Preflight
→ Preserve Prototype
→ Sensitivity/Repair
→ Content Tool
→ Validation
→ Frozen Build
→ Formal
```

Provider Benchmark、共享内核、安全设计可并行；Formal、QC、Evidence 生成不可绕过。

## 11. 页面与接口映射

| 页面 | 路由 | 主要 API |
|---|---|---|
| 生产概览 | `/dashboard` | `GET /dashboard/summary`、`GET /cases/queue` |
| 素材中心 | `/assets` | `GET /assets`、`POST /assets`、`POST /readiness/evaluations`、`GET /manifests/:id` |
| 案例中心 | `/cases` | `GET /cases`、`POST /cases`、`GET /cases/:id` |
| 生成工作台 | `/cases/:id/studio` | `POST /placement/auto-snap`、`PATCH /placement`、`POST /attempts` |
| 质量评审 | `/cases/:id/review` | Development/Validation：`POST /qc-results`；Formal：`GET /formal-runs/:run_id/cases/:case_id/review-results` 只读 |
| Preflight | `/technical/preflight` | `POST /preflight-runs`、`GET /preflight-runs/:id`、`GET /gate-results/:id`、`GET /no-go-reports/:id` |
| 算法与模板 | `/technical/algorithms` | `GET /template-versions`、`POST /template-versions`、`POST /template-versions/:id/freeze` |
| 模型与工作流 | `/technical/workflows` | `GET /models`、`POST /models`、`GET /workflows`、`POST /workflows`、`GET /route-policies`、`POST /route-policies`、`POST /route-policies/:id/freeze` |
| 批量测试 | `/technical/test-runs` | `POST /test-runs`、`GET /test-runs/:id`、`POST /builds/:id/freeze` |
| 效果分析 | `/technical/analytics` | `GET /analytics/repair-evaluation`、`GET /analytics/sensitivity`、`GET /analytics/formal-runs/:id` |
| Formal 执行 | 无新页面，复用 Case/Review/Trace | `POST /formal-runs`、`GET /formal-runs/:id`、`POST /formal-runs/:id/start`、`POST /formal-runs/:id/invalidate`、`POST /formal-runs/:id/review-imports`、`POST /formal-runs/:id/complete` |
| Trace/Replay | `/technical/cases/:id/trace` | `GET /cases/:id/events`、`POST /cases/:id/evidence-packages`、`POST /cases/:id/replays` |

变更接口必须带 `request_id`、`idempotency_key`、`expected_resource_version`；长任务返回 `job_id`，通过 SSE 或轮询读取服务端进度。授权、Freeze 和 Gate 失败统一返回可审计错误码，不能只依赖按钮隐藏。

Formal 产品页始终只读。P0 不建设独立 Reviewer Queue：验收评审人使用冻结模板产生 Review Manifest，由 `POST /formal-runs/:id/review-imports` 以只追加方式导入；服务端验证 case/output hash、Rubric 版本、reviewer 和完整性，并对 `(formal_run_id, output_id, qc_protocol_version)` 建立唯一约束。合法导入后禁止覆盖、改判或删除；输入错误只能在提交前由校验拒绝，不提供改分修正通道。非计分元数据注释可追加，但不得改变五维结果。只有 G0 明确启用 P1 时才设计产品内 Submit-once/裁决交互。

G0 必须将上述资源拆成 OpenAPI 3.1，至少冻结 request/response schema、枚举、分页、幂等、乐观锁、权限、错误码和状态迁移。未通过 API Contract Review 时，只允许搭建骨架，不进入并行业务开发。

## 12. 算法与数据工作包

### 12.0 输入资产与 Pose 默认基线

除非 G0 书面变更，Readiness Manifest 至少要求：

```text
Canonical：正脸 >=1、左45° >=1、右45° >=1；推荐正脸 2–3、左右45°各2
Product View：正面、左右45°、左右侧面、背面、透明 PNG
Product Metadata：view_yaw、view_pitch、material_class、max_residual_yaw/pitch
TRANSLUCENT：白底、灰底、黑底参考，以及 Alpha/Transmission/Specular 分层
```

Pose 初始分区：

```text
Green：|Yaw|<=45° 且 |Pitch|<=15°
Yellow：45°<|Yaw|<=60° 或 15°<|Pitch|<=25°
Red：|Yaw|>60° 或 |Pitch|>25°
Formal 默认 Red=0、Yellow<=20%
```

若 Yellow + Red >20%，必须先完成 Pose Workload Simulation；侧脸、透明/高折射或 Residual 超限只有 Preflight 证明可用后才能进入一期正式范围。

### 12.1 Mouth

- Face/Mouth Landmark 与 Manual Three-point 降级。
- Bite Template、Product View Select、Residual Gate。
- Auto Snap、安全窗口、Mouth Occlusion Preset。
- `M_product/M_core/M_transition/M_contact/M_occlusion/M_visible_core`。
- Contact Edit、Core Restore、局部 Light Match。
- Mouth Attempt 1 Failure 必须互斥归入 Contact-only、Product-only、Mixed、Identity/Scene/Other。

Mouth Repair Pool：

```text
Failure Output >=30
不同 Base Case >=10
单 Base Case最多贡献 3 个输出
```

### 12.2 Hand-held

- Wrist/Palm/Thumb/Index/Middle Landmark。
- Grip Template、Product Grip Anchor、前后 Finger Layer。
- Hand 专属失败码 H01–H06。
- Hand 与 Mouth 分开做 Attempt 1、Repair 和 Bootstrap。

Hand Formal Scope 的最低研发数据：

```text
Hand Development Base Case >=8
不同 Hand Base Case >=5
Hand Failure Output >=15
单 Base Case最多贡献 3 个 Failure Output
```

未满足时 Hand Repair 不得进入 Attempt 2；若 Hand Attempt 1 也不可用，G6 前必须专项整改或书面降级。

### 12.3 Near-mouth

- Product Placement 与 Proximity Gate。
- 默认不启用咬合遮挡。
- Fast Path 只有通过同类稳定性 Benchmark 才能进入正式 Route。

## 13. QC、统计与人工时间协议

所有 Gate 在执行前必须登记 `GateThresholdVersion`：指标、公式、样本来源、判定规则、签署人和版本。允许 Preflight/Sensitivity 产生最终阈值，但“如何从实验结果选择阈值”的规则必须先登记；禁止看到结果后移动 Gate。

### 13.0 默认 Threshold Registry

以下标明来源和效力。“冻结 P0”不得在看到 Formal 结果后修改；“派生 P0”必须按正式方案指定的实验方法在 G4/G6 前得出并冻结；“P1 内部目标”只用于质量观测，除非 G0 书面升格，否则不得成为 Formal 验收门槛。

| Gate | 效力 | 判定规则 |
|---|---|---|
| Same-input Repeatability | 派生 P0 | 5 Base Case × 5 Repeats，报告 Overall/Product/Interaction 标签一致性。出现 PASS/FAIL 翻转时必须在 G2 标记 `CONDITIONAL`，由 Route Policy 记录稳定性风险；是否禁止主链由 G0 预登记规则决定，不再默认以“任一翻转”新增合同性门槛 |
| Reviewer Calibration | P1 内部目标 | 启用双人评审时，至少 20 个非 Formal 分层输出；建议 Overall/五维一致率 >=90%、Cohen's kappa >=0.80。不启用 P1 双审时不执行此 Gate |
| Residual Angle | 派生 P0 | Development 初值 yaw <=12°、pitch <=8°；侧面资产可采用更严值，最终按 Preflight 冻结 |
| Material | 派生 P0 | 冻结背景/视角组合中 Product 与 Scene 均 PASS；任何禁止变化区域结构错误直接 FAIL；TRANSPARENT/高折射默认 Conditional |
| Operation Accuracy | 派生 P0 | Sensitivity 拐点决定 Center/Rotation/Scale Gate；3 名非技术运营 × 5 Gold Case 的 P80 均不得越过对应 Gate |
| Route Triage Accuracy | 派生 P0 | 至少 20 个非 Formal 分层输出，同时覆盖有合法 Repair Route 和无可用 Route 的 Failure，报告 False Pass、False Fail 和 Wrong Route；最终阈值须在测试前登记 |
| Expert Brush | 冻结 P0 | 使用率 <=20%，平均额外时间 <=60 秒 |
| Repair | 冻结 P0 | U_max >=8pp 且 Measured Overall Repair Uplift >=8pp；Mouth/Hand 分开判定 |
| Validation Reliability | P1 内部目标 | 独立可靠性任务数 >=50 时目标 SYSTEM_FAIL Rate <=2%；不足 50 次时只报告样本内观察，不宣称长期失败率。Formal 内的 SYSTEM_FAIL 仍按冻结规则计为 FAIL |

Placement Sensitivity 固定测试档位：Center 为 0、±2%、±5%、±8%、±10%；Rotation 为 0、±3°、±7°、±12°；Scale 为 0、±5%、±8%、±12%。阈值选择规则为：选择在 Interaction/Product/Production Pass 出现不可接受下降前的最后稳定档位，并同时检查 Human Recovery Time；“不可接受下降值”须在实验前写入 GateThresholdVersion。

### 13.1 QC Protocol

G6 前冻结 `qc_protocol_version`，包含：

- 五维逐项 Rubric、正反例和 PASS/FAIL 边界；
- Failure Code 字典及互斥/多选规则；
- Formal Review Manifest Schema：formal_run_id、case_id、output_sha256、qc_protocol_version、五维结果、failure_codes、reviewer_id、submitted_at；
- 每个实际生成 Output 必须有一份验收结果，提交后不允许编辑、撤回、改判、覆盖或删除；
- 一期 Formal QC 页面只读，验收结果通过冻结 Review Manifest 导入；
- 如 G0 启用 P1 双人盲审/裁决，再在 QC Protocol 中增加随机化、隔离、分歧处理和校准规则。

`/cases/:id/review` 在 Formal 中始终只读；Review Manifest 导入、校验、拒绝和非计分注释均写 Audit Event。

### 13.2 Formal Analysis Spec

G6 前冻结：

- Primary estimand：`Technical Pass Case / 全部冻结 Formal Case`。
- 无 VALID_OUTPUT 的 `SYSTEM_FAIL` 无法满足五维全 PASS，计为 Technical FAIL 和 Production FAIL。
- 只有 Gate Signer 按冻结无效条件确认整批 `Test Environment Invalid` 时，才允许将当前 FormalRun 整批标记无效；必须创建带 `supersedes_run_id` 的新 Run，从 Case 1 使用完整原 Dataset 重跑，不得移除单 Case、复用旧输出或混合统计。可复用原 Operator/评审人，但必须记录暴露历史并禁止复用旧结论。
- Production Pass 使用同一 Formal 分母。
- 报告 Technical/Production/Attempt 1/Mouth/Hand/各维度 Pass Rate。
- 二项区间固定为 two-sided Wilson 95% CI。
- Repair 按 Base Case 聚类，至少 1000 次 Bootstrap；固定 Random Seed、分析代码提交摘要和输入 Manifest。
- G4/No-Go 数据优先级固定：Attempt 1 Pass、Failure Prevalence、e、U_max 和 Fast Path 准入读取预冻结 Validation；Measured Overall Repair Uplift 读取预冻结、保持代表性分布的 paired `REPAIR_EVALUATION` Set，对每个 Base Case 比较 Attempt 1 与应用冻结 Repair Policy 后的 Final 结果。只含失败输出的 `REPAIR_POOL` 用于模型开发、Repair Success 诊断和补充不确定性分析，不能直接作为总体 Uplift 分母。Development 仅用于调试和描述。禁止挑选最优数据集、丢弃 Repair 失败或未经预注册地合并；Mouth/Hand 分开判定。`decision_dataset`、Dataset Manifest Hash 和 Build 必须写入 GateResult，且 `formal_data_allowed=false`。
- 8pp 是工程管理阈值，不描述为统计显著性边界。
- Generalization 首批 >=10；Drop >20pp 只触发 Review。Combined >=20 且 Drop >20pp，或两个预冻结 >=10 Batch 均 Drop >20pp，才触发自动降级。

### 13.3 Human Time

`Total Human Time` 定义为操作者主动处理时间：

- 包含正常生产流程中的 Placement、Preset/Mask/Anchor 人工调整、候选选择和单次运营 QC；
- 排除系统推理等待，但单独报告 Wall-clock Time；
- 页面失焦或连续无操作超过冻结 Idle Threshold 的区间不计 Active Time；
- 所有 start/pause/resume/submit 由服务端 Event 计算，禁止人工直接填写总时长；
- Formal 验收时间作为 `Acceptance Review Time` 单独报告，不计入 Production Pass 的单 Case Human Time；如启用 P1 双审/裁决，对应时间也只计入该报告，避免把验收治理成本混入日常生产效率；
- Idle Threshold、日常 QC 边界在 G0 配置中冻结。

## 14. Evidence、Retention 与 DR

每个 Formal Case 的 Evidence Package 必须包含：

```text
Dataset/Input Manifest
Formal Run ID + Build ID + supersedes/invalidation metadata
Original Case Base
Product Asset + Version
Template/Workflow/Model/Container Digest
Placement + Pre-AI Composite
All Required Masks
Every Attempt Raw Output + Provider Metadata
Operation Events + Hash Chain
QC Original Result + Non-scoring Annotations（如有）
Review Result
Replay Manifest
Dataset Reference Audit Result
```

Manifest 计算 SHA256；前端展示 `manifest_sha256`、`object_lock_mode`、`retention_until` 和验证状态。

P0 Evidence Authenticity 依赖 SHA256、服务端 sequence/Hash Chain、对象版本和 Object Lock；Evidence Ready 前必须验证 Manifest 与全部引用对象的 hash/version/retention。独立 KMS 签名和可信时间戳属 P1：仅在 G0 确认客户已提供密钥/权限/时间戳服务时启用，启用后签名失败才阻止 Evidence Ready。

### 14.1 Retention

| Class | 保留策略 |
|---|---|
| Source | 项目期 + 冻结归档期 |
| Formal Evidence / Replay Required | `max(180天, 质保期, 争议窗口)` |
| Validation Intermediate | Freeze 后 90 天 |
| Development Intermediate | Stage Complete 后 30 天 |
| Scratch | 24 小时 |
| Cache | 7 天 |

`expire_at = stage_completed_at + retention_duration`，不得因 `last_accessed_at` 自动延期。延期必须走显式审批。普通数据执行 Soft Delete→7 天 Grace→Hard Delete→Metadata Tombstone；Formal Evidence、Legal Hold 和 Compliance Lock 对象不自动删除。

### 14.2 Object Lock 与 DR

- Bucket 必须在创建时启用 Versioning/Object Lock；锁模式和权限由甲方 IT/合规签署。
- Compliance/Governance 模式、Retention、Legal Hold、时钟同步和管理员绕过权限必须测试。
- PostgreSQL：Daily Backup、Weekly Full；基础设施允许时启用 WAL/PITR。
- 日常初始目标 RPO <=24 小时、RTO <=4 小时，G0 由甲方 IT 确认。
- Formal Window 优先启用 WAL/PITR 或等效连续日志，目标 RPO <=5 分钟；每个 Case Evidence Ready 后写持久化检查点，整批完成后立即生成不可变备份清单。如客户基础设施不支持 5 分钟 RPO，G0 必须在 Formal 前确认等效的单 Case 持久化与恢复方案；冻结方案的日常 RPO <=24 小时/RTO <=4 小时仍是 P0，5 分钟指标不自动升格为合同门槛。
- Restore Drill：恢复数据库、1 个 Formal Evidence、Asset Graph，并成功执行 Evidence Replay 和权限校验。

## 15. 测试、性能与容量

### 15.1 自动化与负向测试

- 单元：Gate、Residual、状态机、Attempt 扣减、统计、Hash Chain。
- 契约：Schema、错误码、乐观锁、幂等、权限。
- 集成：API→Queue/Runner→Worker→Storage→DB；失败注入。
- E2E：素材→Case→Attempt→QC→Freeze→Formal→Evidence→Replay。
- Formal 负向：绕过 UI 修改/删除/重跑、Case ID 注入 Prompt、跨数据集引用、角色越权。
- 一致性：DB 事务失败、对象上传成功但 Finalize 失败、重复消息、孤儿对象。
- 安全：RBAC/唯一账号、Secret 泄漏、依赖/镜像扫描、下载和删除审计；MFA 仅在 G0 启用对应 P1 时进入测试。
- 前端：响应式、Focus Ring、Modal 焦点、加载状态、Formal 只读、Safe/Edge/Unsafe、Cure/No-Go 同步。

### 15.2 性能负载模型

初始单任务 Gate：

```text
Attempt 1 E2E P95 <=120秒
Contact Repair P95 <=90秒
CV Postprocess P95 <=5秒
Multi-band ROI P95 <=2秒
```

G0 冻结目标硬件及业务负载：

- 峰值并发提交数、每小时目标吞吐；
- 最大 Queue Depth 和最长等待时间；
- Worker/GPU 数量与显存；
- 512、1024、最大 ROI，3–6 Pyramid Level；
- P50/P95、错误率、Peak RAM/VRAM、CPU/GPU 利用率；
- Bytes/Attempt、Bytes/Formal Case、Bytes/Evidence、Daily Growth、30 Case 与 90 日容量。

端到端 P95 必须由完整链路实测，禁止把模块 P95 相加。

## 16. Gate 验收表

### 16.1 Formal No-Go 决策矩阵

本矩阵中的 Attempt 1、Failure Prevalence、U_max 和 Fast Path 指标读取预冻结 Validation；Measured Overall Repair Uplift 读取代表性 paired REPAIR_EVALUATION Set；REPAIR_POOL 只用于开发和 Repair Success 诊断。每条结果必须标记 Dataset Manifest Hash 和 Build。Development 只能提供调试证据，Formal Dataset 永远不是准入决策数据源。

| 场景 | 是否进入 Formal | 处理 |
|---|---|---|
| Case Base 不足 | 否 | 补资产；若选择 Base Generation，记录 `OUT_OF_SCOPE_CHANGE` 并暂停本计划；或终止 |
| 核心 Hand Preflight 不通过 | 否 | 补资产、专项研发或书面 Scope Change |
| Attempt 1 >=50%，Repair/Fast Path 不可用 | 可 | 以 Attempt 1 为主，不承诺有效 Attempt 2 |
| Attempt 1 <50%，U_max >=8pp | 条件可 | Measured Repair Uplift >=8pp 后进入 |
| Attempt 1 <50%，U_max <8pp，Fast Path 稳定 | 条件可 | Fast Path 先通过 Validation |
| Attempt 1 <50%，U_max <8pp，Fast Path 不稳定 | 否 | 标记 Formal NO_GO，另立专项技术计划 |
| Hand Repair 样本不足但 Attempt 1 可用 | 条件可 | 禁用 Hand Repair，使用已验证替代路线 |
| Material Preflight 失败 | 否 | 补材质资产、3D 变更或收缩 |
| Residual Angle 超限且无资产 | 否 | 补拍、3D 变更或 Conditional |

### 16.2 Gate 表

| Gate | 通过条件 | 不通过动作 |
|---|---|---|
| G0 Scope/Input | 验收技术口径、数据边界、角色、Readiness 签署 | Cure；范围外 Base Generation 则记录变更并暂停本计划；或终止 |
| G1 Preflight | Face/Product/Material/Residual；核心 Hand 通过 | 补素材、收缩、专项技术变更 |
| G2 Model/Stability | License 合规；完成 5×5 Repeatability 并按预登记规则得出 PASS/CONDITIONAL；版本风险有明确 Route | 淘汰/Conditional |
| G3 Preserve | 商品结构、材质、Contact、Core Restore 可用 | 补视角/材质层、3D 变更或收缩 |
| G4 Accuracy/Repair | Accuracy P80 有实测来源；Repair 输出 GO/NO_REPAIR/CONDITIONAL，并按 No-Go 矩阵形成有效正式路线 | Attempt 1 已达标可取消 Repair；否则使用已验证 Repair/Fast Path，或收缩/变更 |
| G5 Content Tool | E1–E8 产品功能 DoD 完成 | 不进入 Validation |
| G6 Frozen Build | E9/E10 技术 DoD；Config、Analysis/QC Spec、安全/性能、Dataset Manifest 冻结 | 不进入 Formal |
| G7 Formal | 20–30 Case；纪律无违规；按冻结 Spec 出报告 | 如实验收，不删除/调参/补 Attempt |
| G8 Delivery | Generalization、Evidence、DR、Clean-room、培训通过 | 补齐交付或按变更流程处理 |

关键 No-Go：

```text
Attempt 1 <50%
AND U_max <8pp
AND Fast Path 不稳定
```

满足时禁止进入 Formal Performance Acceptance，只能在 G6 前收缩技术范围或另立专项技术计划。

## 17. 交付物

1. Web 前端、API/Runner、Worker、CV/Inference 源码。
2. 数据库迁移、OpenAPI 3.1、状态机、错误码、RBAC 与 Threat Model。
3. Docker Compose、升级/回滚、Secret 配置手册。
4. Readiness、Preflight、Model Benchmark、Sensitivity、Repair 报告。
5. QC Protocol、Review Manifest 冻结模板与校验器、Analysis Spec、Frozen Build/Dataset Manifest。
6. Formal/Generalization 报告与原始可复算数据。
7. Evidence Manifest、Hash Chain、Object Lock、Replay。
8. 自动化测试、性能/容量、安全、备份和 Restore Drill 记录。
9. 管理员、技术管理、内容运营、Formal 执行/验收导入操作手册。

## 18. G0 开发前置配置

- Hand 是否属于 Formal 核心范围及研发资产是否满足最低数量。
- Formal Pose/Interaction 分布、材质、商品视角和 Residual Gate 初始值。
- Human Time、Idle Threshold 和 Expert 操作计时规则。
- 部署硬件、负载、网络、外部 Provider、数据地域和许可证要求。
- Formal Evidence 存储、锁模式、Retention、Legal Hold、RPO/RTO。
- Formal 素材保管/执行人、验收评审人、Gate Signer 和 Auditor 权限；P1 双审启用时再增加 Reviewer/Adjudicator 配置。
- 每个 Gate 的证据和状态转换规则。

任何会改变范围、指标、Attempt、数据集或技术路线的事项必须在 G6 前完成书面变更；Formal 执行中不得临时修改。

## 19. 版本与需求追踪

### 19.1 版本记录

| 版本 | 变化 |
|---|---|
| v1.0 | 初版 Epic、架构、排期、页面/API 映射 |
| v1.1 | 修复 Attempt 语义、Saga、Formal 盲测、安全、统计、Evidence/DR |
| v1.2 | 冻结 Formal Candidate Policy、默认阈值、资产/Pose 基线、Formal RPO 和 KMS 签名 |
| v1.3 | 补齐 FormalRun、非 Formal 决策数据源、paired Repair Evaluation、可靠性样本、分层 Reviewer 校准、Provider 中途漂移、Identity Route、资产权利和 Formal Review SLA |
| v1.4 | 移除非产品内容及相关 Gate，收敛为原型对应的产品开发计划 |
| v1.5 | 区分 P0/P1/范围外，移除默认双审队列和专职角色，KMS/MFA/额外 RPO 降为条件增强，具体化 Formal Review Intake 和 API 准入 |
| v1.6 | 回填 Synthetic Mock v0.2、隔离式 MediaPipe Landmark、Alpha 合成、Pairwise Residual 与材质分层决策证据；明确 Mock 通过不替代真实输入/G0 签署 |
| v1.7 | 冻结全合成正式素材路线和负责人；G0 转 PASS_INTERNAL；新增参数化产品母版、四套独立数据集配额及 Development v1 首批 13 Case |
| v1.8 | Development 补齐 30/30、独立 Validation 20/20 完成输入准入并封存；新增准入/冻结校验、只读状态 API、G2 固定输入准备工具；生产模型基准尚未执行 |
| v1.9 | 按用户指示暂缓模型接入，继续模型无关开发；新增 Development 目录、本地定位与遮罩 API/CLI、可复现预览包与全量渲染检查；发现 1 个非 G2 固定案例的商品视角覆盖缺口 |
| v1.10 | 实现 React Studio、本地交互编辑、PostgreSQL 草稿与模板版本、保存记录哈希校验、容器内前端与新增迁移；项目整体仍未完成 |
| v1.11 | 实现 PostgreSQL 开发任务队列、独立 worker、原子结果保存、五维 QC 版本/导出与并发验证；未改变 Formal/Gate 条件 |
| v1.12 | 按用户确认范围交付本地可用版：唯一账号/RBAC、真实身份、证据包、加密备份与恢复、隔离安装；模型和正式 Gate 继续暂缓 |

### 19.2 需求追踪

建立机器可读 Traceability Matrix：

```text
requirement_id
→ frozen_plan_section / prototype_page
→ epic / API / schema
→ test_case_id
→ gate_result_id
→ evidence_object_hash
```

G6 前要求所有 P0 Requirement 均有实现、测试与证据链接；缺失项自动阻断 Frozen Build。

### 19.3 实施准入清单

G0/G1 可立即开始，不等待其他 Gate：

1. 建立 Repo、分支保护、CI、代码规范、Secret 扫描和最小 Docker Compose 骨架。
2. 完成 `ADR-001` 技术组件验证，记录版本、备选和退出条件。
3. 建立 OpenAPI 3.1 与核心 Schema，先冻结 Dataset/Case/Attempt/Output/QC/Gate/FormalRun/Evidence 的 ID、枚举和状态迁移。
4. 执行输入目录清点、SHA256 工具验证和 Readiness Manifest 预检；只读处理原始资产。
5. 实作 Face/Hand/Product/Material/Residual Preflight Spike，输出可重复运行的报告和原始测量。
6. 创建 P0 Traceability Matrix 初版，将每条需求映射到来源章节、Schema/API、预定测试和 Gate。

G2–G8 全量开发启动前必须同时满足：

- G0 冻结记录已填完，P1 选项全部显式标记 `ENABLED` 或 `DEFERRED`；
- G1 没有未处理的 P0 `BLOCKED` 项；
- API Contract Review 和数据迁移方案通过；
- Development/Validation 资产已就绪，Formal 至少有不暴露内容的数量/分布/hash Manifest；
- 实际到岗人力已按角色人日复核，低缓冲角色有明确时间窗口；
- 负责人发布状态转换记录：`APPROVED FOR G0/G1 → APPROVED FOR FULL IMPLEMENTATION`。
